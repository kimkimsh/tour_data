# 00 — 설정 안내서 (읽는 법)

> **이 폴더가 무엇인가**
> 지금 코드는 다 있는데 **데이터가 하나도 없다.** 화면의 무장애 항목이 전부 「정보 없음」인 이유가 그것이고, 그걸 채우려면 **바깥에서 받아 와야 하는 것이 두 개** 있다.
>
> 이 폴더는 그 두 개를 받아서 어디에 넣는지를 순서대로 적은 것이다. 개념 설명이 아니라 **따라 하면 끝나는 절차**다.

---

## 받아야 하는 것은 두 개다

| 무엇 | 어디서 | 왜 필요한가 | 기다리는 시간 |
|---|---|---|---|
| **한국관광공사 API 키** | 공공데이터포털 | 관광지의 무장애 정보 24항목이 여기서 온다. 이게 없으면 화면이 전부 「정보 없음」이다 | 자동승인 즉시 ~ 1–2일 |
| **Supabase 프로젝트** | supabase.com | 방문자 제보를 저장할 데이터베이스. 없으면 제보 화면이 동작하지 않는다 | 5분 |

**둘은 서로 독립이다.** 하나가 막혀도 다른 하나를 먼저 해도 된다. 다만 **API 키가 훨씬 중요하다** — 그게 이 서비스가 보여줄 내용 자체이고, Supabase는 방문자가 나중에 덧붙이는 부분이다.

---

## 문서 순서

| # | 문서 | 걸리는 시간 | 끝나면 |
|---|---|---|---|
| 01 | [`01_kto_api_key.md`](./01_kto_api_key.md) | 신청 30분 + 승인 대기 | `pnpm probe`가 돌아간다 |
| 02 | [`02_supabase.md`](./02_supabase.md) | 30분 | 제보 화면이 동작한다 |
| 03 | [`03_first_run.md`](./03_first_run.md) | 1–2시간 | **실제 데이터가 화면에 뜬다** |
| 04 | [`04_decisions.md`](./04_decisions.md) | 읽는 데 15분 | 내가 혼자 못 정하는 5가지를 정한다 |

**01 → 03 순서로만 해도 서비스는 돈다.** 02(Supabase)는 제보 기능에만 필요하고, 없어도 나머지 화면은 전부 동작한다.

**04는 키와 무관하다.** 지금 바로 읽고 답해도 되고, 그중 1번은 답이 오는 즉시 화면이 바뀐다.

---

## 지금 상태를 한 번에 보는 법

```bash
pnpm validate:content
```

지금은 이게 **빨간불**이고, 그게 맞다:

```
content validation failed (1):
  - pois.json: 6 place(s) still carry ktoContentId "UNRESOLVED"
```

**이 한 줄이 「아직 API 키를 안 넣었다」는 뜻이다.** 01번과 03번을 끝내면 초록불이 되고, 그때 CI도 같이 초록불이 된다.

---

## 미리 알아 둘 함정 세 개

이 세 개가 실제로 사람들이 걸리는 곳이다. 각 문서에서 다시 나오지만 먼저 적어 둔다.

**1. 한국관광공사 키는 두 가지 형태로 준다. 틀린 쪽을 넣으면 조용히 안 된다.**
「일반 인증키(Encoding)」와 「일반 인증키(Decoding)」가 나란히 보이는데, **Decoding 쪽을 써야 한다.** Encoding 쪽을 넣으면 서버가 `resultCode 30 (등록되지 않은 서비스키)`를 돌려주는데, 이 메시지가 「키가 잘못됐다」로 읽혀서 키를 다시 발급받으러 가게 된다. 키는 멀쩡하고 형태만 틀린 것이다.

**2. Supabase의 익명 로그인은 두 군데서 따로 켜야 한다.**
`supabase/config.toml`에 이미 켜 두었지만 그건 **로컬 개발용**이다. 실제 프로젝트는 웹 대시보드에서 **따로 한 번 더** 켜야 한다. 안 켜면 제보 버튼을 눌러도 아무 일도 안 일어난다.

**3. 마이그레이션은 한 번 올리면 그 파일을 못 고친다.**
`supabase db push`를 하는 순간 Supabase가 「이 파일은 적용됨」이라고 기록하고 다시 실행하지 않는다. 그 뒤로 `002_reports.sql`을 고쳐도 **실제 데이터베이스는 안 바뀐다.** 고칠 게 생기면 `003_*.sql`을 새로 만들어야 한다.

---

## 이 문서들이 다루지 않는 것

- **배포(Vercel)** — 아직 안 했고, 위 두 개가 끝난 뒤의 일이다
- **도메인 연결** — 마찬가지
- **관리자 계정 만들기** — [`02_supabase.md`](./02_supabase.md) 마지막에 SQL 한 줄로 나온다
