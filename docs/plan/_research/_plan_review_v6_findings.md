# 모두의 백제 — Plan Review v6 (multi-lens + Codex + 2026 web re-verify)

> Date: 2026-06-15. Sixth pass. Claude 6-lens + 8-research workflow + independent Codex `gpt-5.5`/`xhigh` (see `_codex_review_v6_raw.md`).
> Calibration: KEEP MVP scope cuts, invest in DEPTH+VALIDATION; SPEC.md canonical. Do NOT re-report resolved B-1..8 / M-1..22 / RC-1..4.
> NOTE: DevEx lens returned a stub in this run and was re-run separately; Eng/Consistency cover the core contract-drift.

## Synthesis verdict

Contest-ready in DIRECTION and architecture (the F1->F5 single-dataset lineage is a genuine differentiator, the locked decisions are coherent, the depth-over-scope calibration is right, and prior rounds closed the formula/evidence/validation/runtime-defect blockers), but NOT yet contest-ready in INTEGRITY of the spine: the single biggest remaining gap is that the join key the whole 'one dataset' thesis depends on — capability_code — is defined four-to-five mutually incompatible ways across the authoritative docs (F-01), so as written the ETL writes one code, F1 reads another, every fact silently becomes 'unknown', and the golden/contract tests are dead-on-arrival. That, plus a cluster of silent-but-demo-fatal contract drifts (HMAC/publish/cache invalidation F-05/06/17/25, F5 denominator F-04, buildDiaryDocument layering F-07, Odii langCode F-02) and two load-bearing UNVERIFIED claims (공산성 wheelchair-traversability seeded before verification F-08, demand asserted-not-observed F-35), means a sharp judge tracing the lineage or watching a live cross-check would today find a different code/number at each hop. None of these touch a SPEC §2 lock or a §7/§16 formula value — they are contract-unification, framing-honesty, and depth/validation hardening, exactly what the generous runway should buy. Fix F-01 first (it gates the golden tests), build the single lineage exhibit (F-12), verify-then-seed the hero POI (F-08), and reframe the two over-claims (F-09/F-10) before they reach slides.


## Top 10 highest-leverage actions

- F-01 (BLOCKER, convergence: eng+consistency+research): Freeze ONE capability_code vocabulary as a single exported constant (doc 16 §2 as authority) BEFORE C1/C2/C4 freeze, and make doc 04 ETL map / doc 05 / doc 03 / doc 09 F5 / doc 13 tests all use it; add a set-equality CI test {ETL emits}=={policy}=={domain switches on}=={F5 reads}. This is the single highest-leverage fix — without it every fact silently maps to 'unknown', F1.A scores everything 정보없음, the golden tests are dead-on-arrival, and the one-dataset thesis collapses on inspection.
- F-08 + F-35 (BLOCKER + observed-demand, office-hours): Run the 공산성 서문 wheelchair on-site traversal check FIRST and let it drive the persona-POI assignment with a named fallback (국립공주박물관) — never seed 'supported' for an untraversed capability; in the same field trip, do 5-8 real 관광약자 interviews to convert the asserted '5 questions' demand into observed pain with verbatim quotes. The hero demo's load-bearing claim and the entire 기획력/적정성 thesis are currently unverified; the generous runway is exactly the place to buy this.
- F-04 + F-67 (BLOCKER + seed coherence): Reconcile F5's completeness denominator to the same frozen 21-capability set F1 scores (exclude the 4 *etc fields), and build a deliberate two-POI seed (공산성 polished hero / 부소산성 improvable) with an E2E assertion tying 부소산성 F1 정보없음 count to its F5 null_fields (AC-F5-06). This is the second-most-likely thing a judge cross-checks live; the F1<->F5 number must match.
- F-12 + F-58 (highest-leverage demo move, ceo): Build the single auditable lineage exhibit as a named, owned deliverable — pick 부소산성 elevator unknown and a one-page 'data passport' showing the identical source_field/verified_at/evidence-pack ID surfacing in F1.A, F1.B, F3, F4, F5, wired as an E2E assertion and backing PT §8.1; add the inline 'where this came from' trace on the F5 top gap row. This is the pre-built answer to the one question that decides the 'one dataset' verdict.
- F-05 + F-06 + F-17 + F-25 (BLOCKER cluster, eng): Freeze ONE revalidate HMAC convention end-to-end (doc-02 body-HMAC, delete the Bearer path), make publish atomicity authoritative via doc-03's delete-then-insert (delete the doc-02 version-filter), fix the F2 docent loader to per-POI tags, and make the ETL emit only registry tags. Otherwise the nightly publish 401s, the cache never invalidates, and the demo serves stale data through the judging window — the 'one dataset flows to all features' pipeline visibly fails.
- F-09 + F-10 (over-claim correction, ceo): Reframe the 데이터활용 headline from '10 services converge into one table' to a hub-and-spoke on poi_id (stronger and defensible), and reconcile the F5 PT 샷6 to the single-gap-table reality while labeling the dashboard '6 POI 기준 갭, 동일 스키마로 확장(발전방향)'. Fix the two biggest over-claims before they reach slides — an overstated convergence/county-coverage invites the disbelief that taints every downstream claim.
- F-11 (highest score-swing, ceo+office-hours): Assign a NAMED human owner to CACF outreach this week with a first-contact date, a pre-drafted LOI, a parallel track to 다도라/올담 or 충남교육청, and a hard auto-downgrade date — and bring the actual 공주/부여 F5 gap report to the meeting as the value proof. The +2 지역특화 + 특별상 is the single largest lever and is currently run as a CI-gate-owned side-task that CI cannot perform.
- F-02 + F-18 + F-19 + F-33 + F-32 (build-breaker cluster, research): Fix the constant/import bugs that detonate silently — add ODII_LANGCODE_MAP (ja->jp, zh-CN->cn1) or the JP/CN docent goes empty; correct braillify imports to translateToUnicode/encode; add a Hanja-capable font fallback or transliterate 史477 or PDFs render tofu; use process.cwd() for react-pdf fonts + WASM output-file-tracing. Each is a one-line-class mismatch that survives review and breaks the multilingual/PDF/braille demo features judges will exercise.
- F-13 + F-14 + F-34 (design depth, the experiential hole, design): Spec the multi-persona selection UX (warm template chips + visible min() 'strictest member' framing — the 기획력-30 differentiator that currently has no UI), the cross-feature Empty/정보없음 states (turn the most-common state into the visible start of F1->F3->F5, not a dead end), and re-tier the 4-Layer card to verdict-first (the math on demand). These are the parts a judge experiences first and the rubric most rewards, and they are pure depth investment within locked scope.
- F-03 + F-37 + F-21 (validation hardening): Resolve doc 16's internal coverage<0.65 label contradiction (§6 주의 vs §9 정보없음) into one decision tree with one-label-per-fixture tests; pilot ONE real F3->moderation->fact-refresh->F5-gap-delta instance to make 발전성 observed not asserted; and replace the flaky whole-file PDF SHA-256 golden with a normalized hash so the 완성도 gate stays green. These convert three credibility signals (reproducible formula, the flywheel, the completeness gate) from asserted/brittle to demonstrably true.


## Research — CHANGED / UNCERTAIN (current-fact corrections)

- KTO-current-state (CHANGED): Odii langCode is ko/en/cn1/jp live, NOT the app's ko/en/ja/zh-CN — plan hardcodes the app Locale as the API param with no mapping (doc 04:110,321; doc 06). Plan impact: add ODII_LANGCODE_MAP {ja:jp, zh-CN:cn1}, fix getDocentStories, probe GATE-5 with jp/cn1. Build/demo-breaker for the JP/CN docent. -> F-02.
- KTO-current-state (CHANGED): TatsCnctrRateService is a 30-day-FORWARD ML FORECAST, not a current/historical crowding measurement; doc 04:125 '혼잡도(상대)' overstates certainty. Plan impact: relabel '예측 혼잡도(향후 30일)' + add 'predicted' provenance flag; no formula change. -> F-61.
- KTO-current-state (CHANGED): legacy areaCode/sigunguCode/cat* deprecation is rolling per-service (90-day after each notice), NO global 2025-12-31 cutoff; the stale cutoff claim still lives in _research/D1 (canonical doc 04 is correct). Plan impact: strike the stale D1 claim + add a CI grep guard against areaCode write filters. -> F-61.
- KTO-operating-account (CHANGED): operating stage is 심의승인 (MANUAL, not auto); 100,000/day is an advertised max requiring 활용사례 registration + a separate increase request (two sequential 심의 round-trips); SLA 2-3일. Plan impact: reword SPEC/04/01/15; demo stays insulated (snapshot-only). -> F-23.
- KWCAG (CHANGED): F2 cites KWCAG check-items by raw WCAG numbers that don't exist in the 33-item KWCAG 2.2 list (1.2.6 수어, 4.1.3, 2.4.3); an a11y judge reads it as WCAG self-assessment. Plan impact: renumber to canonical KWCAG IDs + CI lint on citation strings. -> F-15.
- KWCAG (CHANGED): AiBadge spec (F2:489) violates KWCAG 2.5.3 (accessible name doesn't contain visible label) — the exact rule doc 10 assigns to it — and is also the AI 기본법 §31 disclosure surface. Plan impact: defer to doc 10 §4.3 sample, single-source the badge, golden a11y test. -> F-16.
- KWCAG (CHANGED): wcag22aa is PR-blocking in doc 13 but warn-only in doc 10, and the 9 KWCAG-2.2-NEW mandatory items (several not axe-detectable) have no enforced merge gate. Plan impact: reconcile to one policy + RC-blocking manual checklist for the 9 + a coverage-mapping table. -> F-51.
- KWCAG (CHANGED): K-WAH is a KWCAG-2.0-era tool mis-claimed as the 33-item KWCAG-2.2 인증-신청 필수 서류; F5 cites KWCAG-non-adopted §2.4.11 as a gate. Plan impact: switch to OpenWAX + KWCAG 2.2 표준 점검 지침, fix F5 §2.4.11, add the WA non-profit discount. -> F-50.
- Legal-AI기본법 (CHANGED): the law's grace period defers only 과태료/사실조사, NOT the labeling obligation (effective NOW); plan's PT compliance flex needs the clarifying answer. AND §31(1) 사전 고지 (advance notification) is wholly uncovered — plan implements only §31(2) per-output labels. Plan impact: add onboarding/ToS notice + Q&A line. -> F-30.
- Legal-위치정보법 (CHANGED): 2024 amendment created a 소상공인/1인-창조기업 simplified/deferred-notification path never propagated to canonical doc 11; AND §1.1 asserts 위치기반서비스사업자 status contradicting §1.5 (no raw-GPS persistence). Plan impact: add the path + the eligibility determination, relax the hard 8/31 filing deadline, resolve the classification. -> F-22.
- Legal-AI labeling (CHANGED): F5 PT 샷6 still scripts the CUT heatmap/visitor-chart/report-frequency features as live, contradicting the 09/SPEC §13.2 single-table scope cut — a judge sees charts the spec says don't exist. Plan impact: reconcile 14 샷6 to the GapPriorityTable reality (do NOT restore charts). -> F-10.
- Legal-AI badge (CHANGED): AiBadge has two divergent contracts (variant enum + file path) across doc 11 (voice/translation/course, packages/ui, locale) and doc 06 (audio/translation/route, apps/web, no locale) — a legally-mandated component drifting like pre-B-1 SuitabilityResult. Plan impact: single-source it. -> F-16.
- Legal-PT timeline (CHANGED): §6.1 has zero slack — 7 min D.1 + 4 min supplementary + opening packed into 0:00-12:00 with no buffer for 3 live hero moments; live F4 multi-file export in 60s is physically tight. Plan impact: re-budget with slack, pre-generate or lean on T3 tiering. -> F-63.
- Legal-F4 export labeling (UNCERTAIN->finding): externally-distributed PDFs/BRF carrying AI-translated/narrated content must bear an in-document AI-source notice per §31(2); plan's badge story is on-screen only. Plan impact: embed AI-source notice in exported artifacts. -> F-29.
- Supabase-lineage (CHANGED): FIVE incompatible capability-code vocabularies across docs 16/05/04/03/09; ETL writes one code, F1 reads another, every fact silently 'unknown'. The highest-leverage credibility fix (the join key B-1 left open). Plan impact: freeze one exported constant before C1/C2/C4. -> F-01.
- Supabase-lineage (CHANGED): F5 completeness denominator is 28 raw KTO fields (incl. 4 non-scored *etc) but F1 scores 21 capabilities — AC-F5-06 (F5 null == F1 정보없음) can never hold and the live cross-check mismatches. Plan impact: reconcile F5 field_list to the frozen capability set, exclude *etc, add a contract test. -> F-04.
- Supabase-scope-drift (CHANGED): cut '2박3일' tier still in F4 TimeMode union + doc 01 prose describes the cut 6단 builder in present tense (contradicting its own scope table). Plan impact: fix doc 08 type + doc 01 prose. -> F-44.
- Supabase-build-breaker (CHANGED): F4 student-PDF SHA-256 whole-file golden flakes because pdf-lib/react-pdf embed CreationDate/ModDate by default; the 완성도 gate goes permanently red/disabled. Plan impact: normalized hash + fixed dates/producer. -> F-21.
- Supabase-demo-breaker (CHANGED): F5 PT-stable snapshot is built but the page hardcodes getRtoDashboard('live') with only a code-comment switch -> requires a redeploy on demo day. Plan impact: env/route-param label selection. -> F-43.
- Supabase-credibility (UNCERTAIN): F5 worked-example numbers are hand-authored and 공산성 elevator=0.02 doesn't satisfy the engine's own mapping (~0.006); F1 already bans hand-numbers. Plan impact: golden-generate F5 example numbers + a priority-engine golden test. -> F-42.
- Next-cache (CHANGED): M-15 per-POI cache fix was applied only to doc 02; F2 docent loader (06:521-527) still ships static tags ['docent','poi'] revalidate 3600 -> per-POI docent re-publish silently fails to invalidate. Plan impact: per-POI factory with docent:{poi}:{locale} tags, 86400s, extend the contract test. -> F-17.
- Next-cache (CHANGED): doc 02:288 claims '`use cache` is a no-op when cacheComponents off' — it is actually a hard BUILD ERROR. Plan impact: reword the guard (it's a stronger guard than stated). -> F-49.
- Next-cache (UNCERTAIN): vercel.json functions glob is relative to the Vercel Project Root which must be apps/web, else export functions silently fall to the default timeout. Plan impact: note in §5.1 + RC checklist. -> F-49.
- Supabase/Vercel infra (CONFIRMED but needs propagation): Supabase region ap-northeast-2 is immutable after create (wrong-region silently breaks 국외이전); Free pauses after 7 days no DB activity (keep-alive must be a real SELECT); Vercel Pro is a HARD prerequisite for icn1 Seoul residency, not a convenience; @supabase/ssr 0.12.0 + @serwist/next 9.5.11 current/compatible (pin exactly). Plan impact: provisioning checklist + elevate Pro to launch-blocking. -> F-61/F-49.
- PDF-braille (CHANGED): braillify 2.0.1 exports encode/translateToUnicode/translateToBrailleFont — NOT 'translate' (doc 08) or 'braillify' (doc 06); both call non-existent exports. Plan impact: correct both imports. -> F-18.
- PDF-braille (CHANGED): Pretendard has no Hanja glyphs (maintainer-confirmed) but the plan prints 史477/史13/史5/史301 in all PDF/HTML channels -> tofu. Plan impact: transliterate to Hangul or add Noto Sans KR fallback + '한자 깨짐 0' AC. -> F-19.
- PDF-braille (CHANGED): react-pdf initKoreanFonts() uses a web-absolute '/fonts/...' path that throws ENOENT under renderToBuffer in a Node route (inconsistent with the same file's correct pdf-lib process.cwd() path); 3 of 6 channels render Latin tofu. Plan impact: process.cwd() path + outputFileTracingIncludes + a renders-Korean test. -> F-33.
- PDF-braille (CHANGED): react-pdf Font.register async race (renderToBuffer fires before fonts ready -> intermittent tofu) and pdf-lib CJK subset hazard (subset flag undefined). Plan impact: preload fonts via Buffer before render, set subset:false explicitly. -> F-68.
- PDF-braille (UNCERTAIN): braillify wasm-pack ESM (sync import index_bg.wasm) likely won't trace into the Vercel serverless/ETL bundle without next.config asyncWebAssembly + outputFileTracingIncludes (never configured). Plan impact: add WASM tracing or run braille in ETL and reuse the stored asset. -> F-32.
- Maps-heritage (CHANGED): 문화재청->국가유산청 rename moved cha.go.kr->khs.go.kr; the plan's cited heritage URL 307-redirects to a dead error page, AND the plan asserts 3 different license values for it. Plan impact: replace URLs with khs.go.kr/data.go.kr 15034324, pin the per-endpoint license. -> F-20.
- Maps-heritage (CONFIRMED but needs propagation): Kakao Map free 300k/Local 100k daily + overage 0.1/0.5/2 KRW (80% discount is Talk-Share-only) never stated in canonical docs; juso 좌표제공 approval is unscheduled (no owner); VWorld DEM is security-restricted and must not be the slope source (NGII open DEM is); Kakao Mobility walking-directions is partner-gated and intentionally OUT. Plan impact: propagate cost note + schedule juso approval + tighten VWorld scope + state Kakao Mobility OUT. -> F-62.
- Contest-timeline (CONFIRMED): official 2026 schedule (development 5-9월, 기능심사+PT 10월) exactly matches the plan; KorService OpenAPI usage is mandatory for eligibility (the KTO-spine satisfies it). Reinforces the depth-over-scope calibration; no change. No finding.


## Consolidated findings (all severities)

### F-01 [blocker] (lenses: eng, consistency, research:supabase, ceo (downstream); effort M)
**capability_code has 4+ incompatible vocabularies across docs 16/04/03/05/13 — the lineage join key is undefined, every fact silently maps to 'unknown'**
- Location: 16_suitability_policy.md:65-92 (entrance_step_free/wheelchair_access/tactile_path snake_case) vs 04_kto_data_integration.md:185-200 (MOBILITY_WHEELCHAIR_ENTRY/VISUAL_TACTILE_PAVING SCREAMING_SNAKE) vs 03_data_model.md:488-513 (entry.wheelchair/visual.braileblock dotted) vs 13_testing_quality.md:461-464 (BF_BRAILLE_BLOCK/BF_ENTRY) vs 09_feature_F5_rto_dashboard.md:124-131 (raw KTO keys wheelchair/braileblock)
- Issue: capability_code IS the join key that makes 'one verified dataset flows F1->F5' true. I read the source and confirmed: doc 16 §2 (the declared single authority) uses snake_case domain codes, but its OWN source_field hypothesis points at doc 04's field map which emits SCREAMING_SNAKE codes; doc 03 catalog uses dotted codes; doc 13 contract test (line 461-467) asserts capabilityCode==='BF_BRAILLE_BLOCK' which exists in NO catalog; doc 09 F5 joins on raw KTO field names. Because the codes never match, the domain tier matrix (16 §4.1) keys off codes the ETL never emits -> every fact maps to 'unknown', the F1.A card scores everything 정보없음, and the doc-13 golden+contract tests are dead-on-arrival. This is the single most demo-fatal drift: a judge tracing the dataset hits a different code string at every hop and disbelieves the entire one-dataset thesis. This is the EXACT class B-1 closed for SuitabilityResult but left wide open for the actual KEY.
- Fix: Freeze ONE capability_code vocabulary as an exported constant in packages/domain (doc 16 §2 is the natural owner since it owns the persona-tier matrix) BEFORE any of C1/C2/C4 freeze. Make doc 04's KTO_DETAILWITHTOUR_FIELD_MAP.capabilityCode emit doc-16 codes (drop SCREAMING_SNAKE), rewrite doc 05 §2.2 to doc-16 codes, delete the dotted catalog from doc 03, change doc 09 F5 to JOIN on accessibility_facts.capability_code (not source_field), and regenerate the doc-13 assertions from the doc-16 list. Add a CI set-equality test: {codes emitted by ETL} == {codes in suitability-policy-v1.json} == {codes the domain switches on} == {codes F5 reads}. Until unified, no golden test can be authored.

### F-02 [blocker] (lenses: research:kto-current-state; effort S)
**Odii langCode enum is ko/en/cn1/jp (live-verified), NOT the app's ko/en/ja/zh-CN — getDocentStories passes the app Locale straight through, so ja/zh-CN docent fetches silently return empty**
- Location: 04_kto_data_integration.md:110 (hardcodes langCode=ko/en/ja/zh-CN) + 04_kto_data_integration.md:321 (getDocentStories(langCode: Locale)) + 06_feature_F2_docent.md:205,162,438,548
- Issue: Research live-verified the Odii spec (data.go.kr 15101971, guide v4.1, edited 2026-02-25): langCode = exactly ko / en / cn1 (Simplified Chinese) / jp (Japanese). The plan hardcodes ko/en/ja/zh-CN everywhere and the typed client signature getDocentStories(langCode: Locale) treats the app Locale type AS the API param with NO mapping (I read doc 04 line 110 — it even says 'contentTypeId 언어매핑 없음' but provides no langCode mapping). If shipped unmapped, every Odii fetch for Japanese and Chinese returns bad-param/empty, GATE-5 records false 'no coverage' for ja/zh-CN, and the F2 docent silently falls back to TTS for jp/cn1 at all 6 POIs — hollowing out the 4-language multilingual docent that judges will demo. A build/demo-breaker hiding behind a constant.
- Fix: KEEP app/DB Locale = (ko,en,ja,zh-CN). ADD an isolation constant ODII_LANGCODE_MAP = { ko:'ko', en:'en', ja:'jp', 'zh-CN':'cn1' } in packages/kto-client/src/maps (same pattern as KTO_DETAILWITHTOUR_FIELD_MAP), change getDocentStories to accept app Locale and translate internally (never pass Locale straight through), and fix doc 04 line 110, AC-10, and doc 06 ETL/flow references. gate:0 GATE-5 must probe with jp/cn1, not ja/zh-CN, or its coverage matrix is invalid.

### F-03 [blocker] (lenses: consistency; effort S)
**doc 16 self-contradicts on coverage<0.65 label: §6 caps at '주의' but §9 step 2 says '정보없음' — same authoritative doc, two labels for identical input; acceptance test cannot pin a value**
- Location: 16_suitability_policy.md:186 (coverage<0.65 caps label at 주의) vs 16_suitability_policy.md:206 (coverage<0.65 -> label 정보없음); propagated to 05_feature_F1_total_guide.md:489-490; 13_testing_quality.md:258-262
- Issue: I read both lines in the single authoritative doc 16. §6 M-2 guard (line 186): 'if coverage < 0.65, the label is capped at 주의 regardless of score'. §9 forced-rule step 2 (line 206): 'else if any selected-persona critical = unknown OR coverage < 0.65 -> label 정보없음'. For an identical low-coverage input these produce different labels. doc 05 §4.4 faithfully reproduces BOTH, and the doc 13 acceptance test enshrines the ambiguity as expect(['주의','정보없음']).toContain(result.label) — an assertion that passes for either label, making the criterion untestable as a regression gate. A judge probing reproducibility finds the engine's own spec cannot decide its output.
- Fix: Resolve into one decision tree: coverage<0.65 WITH a critical-unknown -> 정보없음; coverage<0.65 WITHOUT a critical-unknown -> cap at 주의. Rewrite §6 and §9 so they are a single ordered rule, then split the doc-13 test into a critical-unknown fixture asserting 정보없음 and a non-critical low-coverage fixture asserting 주의 (one exact label each).

### F-04 [blocker] (lenses: research:supabase, ceo, consistency; effort M)
**F5 completeness denominator is 28 raw KTO fields (incl. 4 non-scored *etc), but F1 scores 21 capabilities — AC-F5-06 (F5 null == F1 정보없음) can never hold, and the demo cross-check a judge runs will mismatch**
- Location: 09_feature_F5_rto_dashboard.md:124-131 (array of 28 entries, comment falsely says '21개') + 09:241-247 (total_fields=28) vs 03_data_model.md:514 (*etc fields NOT scored) + 09:896-900 (AC-F5-06)
- Issue: I read doc 09 lines 120-131: the field_list comment says '21개 detailWithTour2 핵심 필드' but the array literally contains 28 entries, including the 4 *etc catchall fields (handicapetc, blindhandicapetc, hearinghandicapetc, infantsfamilyetc) and non-capabilities (promotion, guidesystem). doc 03 §3.5 explicitly states the *etc fields are detail-text only, NOT scored capabilities. So F5's denominator (28) can never equal F1's capability count (21), F5's null_fields systematically overcounts vs F1's 정보없음, and F5's own AC-F5-06 closed-loop proof breaks. This is the second-most-likely thing to make a judge disbelieve the closed loop during a live cross-check of the F1.A card vs the F5 dashboard — the precise F1<->F5 payoff. Compounds F-01 (F5 also joins on raw keys, not domain codes).
- Fix: Reconcile F5's field_list to the SAME frozen capability_code set F1 scores (the F-01 unified catalog), compute completeness over scored capabilities only, and either exclude the 4 *etc fields or carry them as a separate visibly-non-completeness 'detail-only' count. Fix the false '21개' comment and the hardcoded total_fields=28 (lines 241/245) and OI-4 size estimate (line 923). Add a contract test asserting F5.completeness denominator === F1 scored-capability count for a fixed POI (공산성) so AC-F5-06 is mechanically enforced.

### F-05 [blocker] (lenses: eng; effort S)
**Three divergent HMAC revalidate conventions (header/env/auth-style all differ) -> guaranteed 401/403 on every nightly publish; cache never invalidates, demo serves stale data through the judging window**
- Location: 02_architecture.md:398,404,424,428 (x-revalidate-signature + REVALIDATE_HMAC_SECRET, body-HMAC) vs 04_kto_data_integration.md:398-409 (x-etl-signature + ETL_HMAC_SECRET) vs 13_testing_quality.md:1453-1457 (Authorization: Bearer REVALIDATE_HMAC_TOKEN — static token, not body-HMAC)
- Issue: The same /api/internal/revalidate endpoint is specified three incompatible ways: doc 02 body-HMAC with x-revalidate-signature + 5-min skew check; doc 04 §6.3 x-etl-signature + ETL_HMAC_SECRET, 403, no skew; doc 13 §8.2 — the canonical copy-pasteable kto-etl.yml workflow that will actually run — uses Authorization: Bearer <REVALIDATE_HMAC_TOKEN>, a third secret name and a static bearer (not an HMAC of the body, and replayable). Whichever handler is built, the workflow's call fails its check -> 401/403 every nightly publish -> revalidateTag never runs -> stale POI/score data served until the 86400s TTL, i.e. through an entire demo window. Env-name drift (3 names) means even a correct handler reads an unset env and 500s.
- Fix: Freeze ONE convention end-to-end: doc-02 body-HMAC with timestamp skew (only replay-resistant one). Rewrite doc 04 §6.3 and the doc 13 §8.2 workflow curl to sign the JSON body and send x-revalidate-signature using exactly REVALIDATE_HMAC_SECRET (fix the §9 env table). Add the contract test promised in 02 §11 (missing/wrong/stale/tampered -> 401, valid -> tags revalidated). Delete the Bearer path.

### F-06 [blocker] (lenses: eng; effort M)
**Atomic-publish 'last-good-served' guarantee is unimplementable: read-model version filter (doc 02) references a published_version column that doesn't exist and contradicts publish_dataset()'s delete-then-insert (doc 03)**
- Location: 02_architecture.md:387,368-382 (WHERE published_version=(...active=true) + dataset='main'+active col) vs 03_data_model.md:201-257,403-417 (publish_dataset() delete-then-insert on live table; dataset_versions PK (dataset,published_version) + row_status, NO active col, NO published_version on pois/accessibility_facts)
- Issue: The ETL-failure-isolation story a judge will probe rests on three mutually exclusive specs. doc 02 §6.2(c) mandates read queries add WHERE published_version=(SELECT...active=true) — but pois/accessibility_facts have NO published_version column, so the clause cannot compile. doc 03's authoritative publish_dataset() does delete-then-insert full-snapshot on the SAME live table (one version's rows ever; the version filter is dead). The two docs even disagree on dataset_versions' identity (single dataset='main'+boolean active vs PK (dataset,published_version)+row_status). The central durability invariant cannot be built as written.
- Fix: Pick the doc-03 model as authority (it owns DB contract + handles tombstones/mid-batch failure). DELETE doc 02 §6.2(c) version-filter sentence and its pseudo-SQL, replace with a pointer to publish_dataset() + row_status. State explicitly that 'last-good-served' comes from transaction atomicity of delete-then-insert (rollback leaves the live table untouched), NOT a version pointer. Add the acceptance test asserting the read path uses no published_version predicate.

### F-07 [blocker] (lenses: devex; effort M)
**buildDiaryDocument is declared a pure-domain function but its F4 spec returns PDF/BRF Buffers and calls packages/exports (react-pdf, braillify) — fails dependency-cruiser CI on day one, on the Window-1 slice critical path**
- Location: 08_feature_F4_diary.md:197-234 (buildDiaryDocument in packages/domain/diary, returns Buffer, body calls PDF/BRF builders) vs 02_architecture.md:126,159 (5 pure-domain fns, 0 deps, domain forbidden from importing react/exports — dependency-cruiser error) + 12_workstreams:108 (renderer is CX-owned)
- Issue: doc 02 and doc 12 declare buildDiaryDocument as a pure-domain function in packages/domain with zero deps and an explicit HARD-RULE dependency-cruiser ban on domain importing react/packages-exports. But doc 08 places it in packages/domain/diary with a Buffer return type and a body that calls each PDF/BRF builder — those live in packages/exports (react-pdf, pdf-lib, braillify). A builder following F4 fails CI immediately. C0, CX, and F4 owners each get a different model, on the critical path of the first vertical slice.
- Fix: Split it: keep a pure-domain buildDiaryDocument returning a framework-free DiaryDocument IR (the 6-output common IR) with NO Buffer field; CX renderers in packages/exports take that IR and produce buffers. State once that the export route calls domain then CX, and domain never imports exports. Delete the buffer-returning signature from doc 08.

### F-08 [blocker] (lenses: office-hours; effort M)
**Hero demo POI 공산성 is a hilltop fortress whose wheelchair-traversability is SEEDED as 'supported' before July on-site verification — no fallback POI if verification falsifies it**
- Location: 05_feature_F1_total_guide.md:497 (seeds wheelchair_access supported, exit_step_free supported) + 14_demo_pt.md:118 (6deg slope detour at 만하루), 311-312 (공산성 전체 필드 완비) + 15_risks_open_items.md:68 (R-V1 July verification)
- Issue: The entire D.1 golden flow rests on a wheelchair 70대 할아버지 completing 반나절 공산성, a hilltop 산성 with a documented 6deg (~10.5%) slope detour. Yet the worked example seeds wheelchair_access=supported and the demo checklist commits to '공산성 전체 필드 완비' — the demo asserts the answer is YES before the on-site 관광약자 verification (scheduled July) that would confirm a real wheelchair user can traverse 서문->만하루. If verification finds 공산성 is not step-free enough, the primary hero narrative is false with no named alternative. A judge who has pushed a wheelchair up a 산성 disbelieves a flawless wheelchair demo on 공산성 instantly. Compounds CEO-6 (the two-POI seed must be internally consistent).
- Fix: Pull the 공산성 서문 wheelchair on-site traversal check AHEAD of the July pass and let the result drive the persona-POI assignment, not the reverse. Define an explicit fallback: if 공산성 is not honestly traversable end-to-end, demo the wheelchair할아버지 on the flatter 국립공주박물관 and use 공산성 to honestly show 본질제약 + 대체추천 (a STRONGER 정직성 story). Branch the demo script on the verified result; never seed 'supported' for a capability the team has not physically traversed.

### F-09 [major] (lenses: ceo, research:supabase (lineage); effort S)
**데이터활용 headline '10 KTO services converge into accessibility_facts' is false on inspection — only detailWithTour2 writes that table; the convergence diagram invites disbelief that taints every downstream claim**
- Location: 01_overview_goals_scope.md:273 (§5.2) vs 04_kto_data_integration.md:86,175-177 (only detailWithTour2 writes accessibility_facts)
- Issue: The 데이터활용-20 slide says '10개 KTO 서비스가 accessibility_facts라는 단일 정규화 테이블로 수렴'. Per doc 04, ONLY KorWithService2.detailWithTour2 writes accessibility_facts; the other 9 services feed separate tables (Odii->docent_assets, TatsCnctr->context_snapshots, DataLab->datalab_snapshots, etc.). A judge who reads the funnel diagram and asks 'show me the 10 services landing in this one table' finds a single-source table. An overstated convergence is worse than an accurate narrower one.
- Fix: Reframe as a hub-and-spoke on poi_id (a STRONGER, defensible story): 'accessibility_facts is the single decision spine fed by the one authoritative accessibility source; the other KTO services attach as typed context layers (Odii docent, TatsCnctr/KMA timeContext, DataLab gap-context, PhotoGallery evidence, TarRlteTar relateds) that all key off the same poi_id.' Draw the slide as spokes into poi_id, not a funnel into one table.

### F-10 [major] (lenses: ceo, office-hours, research:legal; effort M)
**F5 presents 시군-level numbers ('공주 미입력 12건') computed over only 3 curated POIs, AND the PT 샷6 still scripts the CUT heatmap/visitor-chart/report-frequency features — F5 over-reaches its dataset and contradicts its own scope cut**
- Location: 14_demo_pt.md:194-197 (샷6 scripts heatmap/DataLab chart/report-frequency), 317 (seeds 공주 12건/부여 9건) + 09_feature_F5_rto_dashboard.md:570-639,705-720 (MVP = single gap table, charts cut to 발전방향) + 09:870 (AC-F5-02 aggregates 3 POIs) vs proposal.txt:33 ('시군별 자동 식별')
- Issue: Two compounding defects on the F5 hero close. (1) The proposal promises county-wide '시군별 자동 식별'; the demo shows '공주 미입력 12건/부여 9건' as if county stats, but F5 aggregates over exactly 3 POIs per 시군 — a judge who sees '공주시' containing only 3 sites reads the framing as inflated. (2) doc 09 was correctly rewritten to SPEC §13.2 (MVP = single gap-priority table; heatmap/visitor-trend/report-frequency REMOVED to 발전방향), but 14 샷6 still scripts those cut charts as live — the live hero moment shows charts the spec says don't exist. The two canonical docs disagree on what F5 IS at demo time; this is the F5 payoff and it visibly over-reaches.
- Fix: (a) Reconcile 14 샷6 + §5.2 checklist to the 09/SPEC single-gap-priority-table reality (GapPriorityTable rows + CSV + DataCaveatFooter; do NOT restore charts). (b) Make F5 scope-honesty a feature: label as 'MVP 검증 대상 6 POI 기준 갭; 전 시군 확장은 동일 스키마로 POI 추가만 필요(발전방향)', replace absolute counts with per-POI rows and a visible '검증 POI 수: 3' denominator. Widen the honesty of the label, not the MVP scope.

### F-11 [major] (lenses: ceo, office-hours; effort M)
**CACF 특별상 / +2 지역특화 — the single largest score swing — has NO named human owner, only stream codes, with one soften-the-wording fallback and no LOI drafted/contacted**
- Location: 15_risks_open_items.md:334 (OI-9, Owner = C2/C0/Q0 stream codes), :24 (R-D9 P0) + 14_demo_pt.md:203 + 01_overview_goals_scope.md:83
- Issue: 지역특화 +2 (on a 100-pt 1차) + CACF RTO 특별상 candidacy is one of the largest single levers, called a P0 (R-D9). Yet OI-9 ('CACF LOI 수령, 7월 말') lists only stream codes as Owner — no accountable human, no first-contact date, no contact path, no escalation if 7월 말 slips, no Plan-B recipient. The only fallback is softening slide wording. CI cannot send an email or attend a meeting; treating a board-relationship procurement task as a CI-gate owner is a category error. There is no evidence CACF has been contacted.
- Fix: Assign a NAMED human owner THIS week with: (a) a first-contact target date (not just '7월 말 수령'), (b) the LOI text drafted in advance so CACF only signs, (c) a parallel track to 다도라/올담 operators (or 충남교육청/한국장애인개발원) as alternate RTO recipients, (d) a hard decision date by which every B2G slide auto-downgrades to 'designed for RTO handoff'. Bring the actual 공주/부여 F5 gap report to the meeting as the value proof, and capture one buyer quote on what format they need (make F5's output schema match the buyer, not a guess).

### F-12 [major] (lenses: ceo; effort M)
**No single auditable lineage exhibit exists as a build artifact — prior reviews named it the highest-leverage move, but it is only a static slide diagram + a functional slice, not one real record traced identically through all 5 features**
- Location: 14_demo_pt.md:441-460 (§8.1 static diagram) + _research/_plan_review_findings.md:310 vs 15_risks_open_items.md (no Open Item, no owner)
- Issue: The consolidated review concluded the single highest-leverage move is to show ONE auditable lineage line (photo/measurement -> accessibility_fact -> 산식 bottleneck -> 서문 route step -> diary record -> F5 priority) and make THAT bulletproof. The first slice builds the functional path and §8.1 is a static diagram, but nothing commits to a single real end-to-end traceable record carrying identical provenance (same source_field + verified_at + evidence-pack ID) through F1.A deduction, F1.B route hazard, F3 report linkage, F4 diary note, F5 top priority. The PT relies on asserting 'the same row appears everywhere'; the killer question 'prove this exact elevator fact is the same one driving F1, F3, F5' has no pre-built answer.
- Fix: Make 'the lineage exhibit' a named deliverable with an owner: pick ONE fact (부소산성 elevator unknown — already the F5 priority=1.00 hero) and build a one-page 'data passport' showing the identical source_field/verified_at/evidence-pack ID surfacing in F1.A, F1.B, F3, F4, F5. Wire it as an E2E assertion (extend first-slice.spec.ts) so it cannot rot, and make it §8.1's live backing.

### F-13 [major] (lenses: design; effort M)
**Multi-persona selection — the first interaction and the 기획력-30 differentiator — has no designed UI; exists only as a bare <PersonaSelector> name, and the live demo entry it relies on is tap-based (not the STT it showcases)**
- Location: 05_feature_F1_total_guide.md:533 (bare <PersonaSelector personaIds />) + 14_demo_pt.md:73-80 (샷0 personas auto-selected from STT, but §13.8 live entry is tap)
- Issue: The product thesis is 'wheelchair grandfather + senior + granddaughter in ONE flow.' That selection is the literal first thing a user/judge touches and the rubric explicitly scores it (기획력 30). Yet it exists only as a one-line component name with zero interaction spec: no answer to how 3 personas + 외국인 toggle + P3 인지옵션 get chosen on a phone without a complexity explosion, no default grouping, no visual model for the min() 'strictest member' rule that is the product's intellectual core. The PT papers over it with STT auto-select, but the live demo uses tap — so the tap persona UI a judge actually sees is undesigned. A bare 8-toggle jargon checklist is the opposite of barrier-free and makes the min() story invisible.
- Fix: Add a 'Persona Selection UX' spec to doc 05 §2: a human-language '누구와 함께 가시나요?' entry with 2-3 warm illustrated TEMPLATE chips that expand to persona codes, plus a '직접 고르기' path; selecting two templates visibly merges into one card with plain-language 'OO님 기준으로 가장 엄격하게 안내합니다' that makes min() a selling point; 인지옵션/외국인 as progressive toggles. Specify the tap flow, merged-state copy, aria pattern, and 'change party' affordance.

### F-14 [major] (lenses: design; effort M)
**Empty / 정보없음 / zero-result / first-run states are undesigned across all 5 features — in a barrier-free product '정보없음' is the COMMON state, and as written it reads as a dead end that makes the dataset look thin**
- Location: 05_feature_F1_total_guide.md:25,330,491 (rule restated, never designed) + 16_suitability_policy.md:206-209 + 09_feature_F5_rto_dashboard.md:906 (AC names '빈 상태 UI', specifies nothing) + 08_feature_F4_diary.md (no empty-diary/first-run)
- Issue: The dataset's central honesty is 'NEVER infer — show 정보없음', so the empty state is the most-shown state, not an edge case. The plan repeats the data RULE everywhere but never designs the EXPERIENCE of (a) 정보없음 on the hero card, (b) 대체추천 with no qualifying alternatives, (c) first-run home with no persona, (d) empty diary, (e) F5 with no live snapshot. As written, unknowns render as a wall of gray '정보 없음' — which a judge reads as 'their dataset is thin', the exact opposite of the F1->F5 thesis. A barrier-free product that turns its most common state into a dead end fails the population it serves.
- Fix: Add an 'Empty & Unknown States' cross-feature primitive (doc 10, referenced by 05/08/09): 정보없음 becomes '아직 확인되지 않았어요 — 현장 확인이 필요해요' with (a)본질제약 styled gray-calm vs (b)운영자 미입력 styled actionable-amber, plus one-tap '이 항목 제보하기' (into F3) and '비슷한 검증된 곳 보기' — turning a gap into the visible start of F1->F3->F5 in front of the judge. Specify empty-diary first-run, no-alternatives copy, F5 no-snapshot state, and a rule that an all-unknown card still shows the 'why we don't guess' trust line.

### F-15 [major] (lenses: research:kwcag; effort S)
**F2 docent cites KWCAG check-items by raw WCAG numbers that don't exist in the canonical 33-item KWCAG 2.2 list (1.2.6 수어, 4.1.3, 2.4.3) — an a11y-literate judge concludes the team self-assessed against WCAG, not the claimed KWCAG 2.2**
- Location: 06_feature_F2_docent.md:481,483,484,486,696,697,704,705 (1.2.2/1.2.6/4.1.2/4.1.3/2.4.3) vs 10_accessibility_kwcag.md:24,31,45,77 (canonical 33-item KWCAG 2.2 numbering)
- Issue: Research verified KWCAG 2.2 = KS X OT0003, 4 원칙/14 지침/33 검사항목, and KWCAG<->WCAG numbers are explicitly non-1:1 (doc 10 line 24). F2 §8.3/§15.2 cite '1.2.6 수어' (KWCAG has no separate 수어 item — folded into 1.2.1), '4.1.3 상태 메시지' and '4.1.2' (KWCAG collapses both into 4.2.1), '2.4.3 초점 순서' (KWCAG focus order is 2.1.2; KWCAG 2.4.3 is link text). These are pure WCAG SC numbers in none of the KWCAG 33. A judge reading them concludes WCAG self-assessment, undermining the KWCAG 2.2 자체점검 the PT leans on.
- Fix: Renumber all F2 §8.3/§15.2 references to canonical KWCAG 2.2 IDs from doc 10 §1.1 (자막->1.2.1, 수어 텍스트 대안->1.2.1, 상태 메시지/볼륨 슬라이더->4.2.1, 초점 이동->2.1.2). Add a CI/lint rule that every 'KWCAG' citation string in the repo is a member of doc 10's 33-item set so feature docs cannot drift to WCAG numbering again.

### F-16 [major] (lenses: research:kwcag, research:legal (AI badge); effort S)
**AiBadge violates KWCAG 2.5.3 (the rule doc 10 assigns to it): accessible name doesn't contain visible label; AND two docs define AiBadge with different variant enums + file paths — a legally-mandated component is both broken and drifting**
- Location: 06_feature_F2_docent.md:489 (aria-label!=visible text), 645-667 (variant audio/translation/route, apps/web path, no locale) vs 10_accessibility_kwcag.md:57,123,492-508 (2.5.3 rule + correct sample) vs 11_legal_compliance.md:358-382 (variant voice/translation/course, packages/ui path, locale prop)
- Issue: Two compounding defects on the AI-disclosure component, which is both an a11y artifact and the AI 기본법 §31(2) mandatory-labeling surface. (1) F2 line 489 specs <span aria-label='AI가 생성한 음성입니다'>AI 음성 안내</span> — the accessible name doesn't contain the visible text (textbook 2.5.3 violation, in the exact component doc 10 flagged for 2.5.3), and aria-label on a non-interactive span is ignored by axe-core. doc 10 §4.3's own sample is correct. (2) doc 11 declares AiBadgeVariant = voice|translation|course at packages/ui with a locale prop; doc 06 declares audio|translation|route at apps/web with no locale. voice!=audio, course!=route, different paths — two incompatible definitions of a legally-required component, the exact drift class B-1 closed for SuitabilityResult.
- Fix: Single-source AiBadge like SuitabilityResult: one path (packages/ui per doc 11), one variant enum, decide locale prop yes/no, make doc 06 link rather than re-declare. Fix the accessible-name to contain the visible label (defer to doc 10 §4.3) and render on a focusable/role'd element. Add a golden a11y test asserting accessible-name contains visible-label for every badge variant.

### F-17 [major] (lenses: research:next-cache; effort S)
**M-15 per-POI cache fix was applied only to doc 02; the F2 docent loader still ships the pre-fix broken static tags ['docent','poi'] revalidate 3600 — per-POI docent re-publish silently fails to invalidate, serving stale content during judging**
- Location: 06_feature_F2_docent.md:521-527 (static tags 'docent'/'poi', no poi:{id}/docent:{poi}:{locale}, revalidate 3600) vs 02_architecture.md:251,264-282 (docent_assets must be docent:{poi}:{locale}, 86400s)
- Issue: I read doc 06 lines 521-527 and confirmed it still uses unstable_cache with literal tags ['docent','poi'] and revalidate 3600. The ETL's documented invalidation trigger revalidateTag('docent:gongsanseong:ko') will not match the literal 'docent' tag, so a per-POI docent re-publish silently fails to invalidate and serves stale data until TTL — directly contradicting the 02 §4.2 contract and the M-15 resolution. The cache-invalidation contract test only covers poi:{id}, so it would not catch this F2 regression. Also revalidate mismatch (06=3600s vs 02=86400s). Same class as F-05 but a separate concrete bug.
- Fix: Fix doc 06:521-527 to a makeLoadDocentData(poiId, locale) factory with keyParts ['docent', poiId, locale] and tags [`docent:${poiId}:${locale}`, 'docent:all'], revalidate 86400, matching the 02 §4.2 table exactly. Extend the cache-invalidation contract test to assert docent tag granularity so this drift is caught structurally. (See also F-25: ETL emits route:all/snapshot:latest tags that don't exist in the registry — same tag-namespace family.)

### F-18 [major] (lenses: research:pdf-braille; effort S)
**braillify 2.0.1 exports encode/translateToUnicode/translateToBrailleFont — NOT 'translate' (doc 08) or 'braillify' (doc 06); two docs invent two different wrong import names, neither compiles**
- Location: 08_feature_F4_diary.md:462,479 (import { translate }) + 06_feature_F2_docent.md:289,296 (import { braillify })
- Issue: Research verified braillify 2.0.1 (current latest, 2026-05-26) ships exactly encode(text):Uint8Array, translateToUnicode(text):string, translateToBrailleFont(text):string. doc 08 imports { translate } and calls translate(plainText); doc 06 imports { braillify }. Both call non-existent exports — build-breakers on the F4/BRF critical path, and the two docs disagree on the wrong name.
- Fix: Correct both call sites: translateToUnicode() for screen/.txt, and re-evaluate whether the manual U+2800->ASCII offset mapping (08:518-525) is still needed vs encode() (the embosser byte form). Pin one canonical import in one place; the F2->F4 reuse note means both must agree.

### F-19 [major] (lenses: research:pdf-braille; effort M)
**Pretendard has NO Hanja glyphs (maintainer-confirmed), but the plan prints heritage designations 史477/史13/史5/史301 in all PDF/HTML channels — they render as tofu, with an a11y 'no broken glyph' AC that wouldn't catch it**
- Location: SPEC.md §3 (史477/史13/史5/史301) + 08_feature_F4_diary.md:351-360,792 (all react-pdf + HTML embed only Pretendard) + 06 heritage_ref 史477
- Issue: Research verified via orioncactus/pretendard #114: base Pretendard deliberately excludes Hanja. The plan uses Hanja heritage numbers throughout (SPEC §3 designations, doc 08 rubric heritage_ref, F2 quiz). All four react-pdf channels and the HTML fallback embed only Pretendard, so any Hanja renders as tofu (square box) in the PDF; the HTML stack only survives on Apple devices. The student PDF submitted to a school would show broken heritage references.
- Fix: Either (a) transliterate Hanja to Hangul before render (render '사적 제477호', store raw Hanja for data) or (b) register a Hanja-capable fallback (Noto Sans KR static, already listed) as a second family in initKoreanFonts and HTML @font-face. Add an acceptance criterion '한자 깨짐 0' alongside the existing '한글 깨짐 0'. pdf-lib student PDF has the same exposure.

### F-20 [major] (lenses: research:maps-heritage; effort S)
**문화재청 was renamed 국가유산청 and the domain moved cha.go.kr -> khs.go.kr; the plan's cited heritage source URL now 307-redirects to a dead error page — a judge who clicks it disbelieves the '국가유산청 원문' pillar**
- Location: 04_kto_data_integration.md:425 + 06_feature_F2_docent.md:229 + 01_overview_goals_scope.md:186 (cha.go.kr references) — live cha.go.kr/openapi -> khs.go.kr/error_page.html
- Issue: Research verified (2026-06): 문화재청->국가유산청 rename 2024-05-17, domain cha.go.kr->khs.go.kr; cha.go.kr/openapi 307-redirects to a dead khs.go.kr/error_page.html. The data.go.kr listing (15034324) is live and stamped 이용허락범위 제한없음, but its endpoint field still points at the dead cha.go.kr URL. The plan cites cha.go.kr as the heritage source in 04/06/01. A judge clicking the cited source lands on an error page, directly undermining the F2/F4 '국가유산청 원문' pillar. Separately the plan asserts THREE different license values for it (공공누리 1·4 혼재 / Type1 추정 / 제한없음).
- Fix: Replace every cha.go.kr/openapi reference with the khs.go.kr OpenAPI list URL (or call via data.go.kr 15034324) and rename source labels (04:425, 06:229, 01:186, SPEC §2.15 if present). Resolve the license contradiction by pinning the per-endpoint license actually shown on the live page (the F4-PDF 're-design forbidden for Type4' rule hinges on this unverified value). Heritage is an ETL snapshot so runtime is unaffected — but reviewer trust is the stake.

### F-21 [major] (lenses: research:pdf-braille; effort M)
**F4 student-PDF golden test hashes the whole PDF with SHA-256, but pdf-lib/react-pdf embed CreationDate/ModDate by default — the gate flakes on day one, undermining the very 완성도 evidence it provides**
- Location: 08_feature_F4_diary.md:1152-1158 (createHash sha256 of buffer == golden), 1172 (AC), 1184
- Issue: The plan gates CI on byte-identical PDF output (SHA-256 of the buffer) and leans on it as 완성도-30 evidence, but pdf-lib and @react-pdf/renderer embed /CreationDate and /ModDate in the Info dict by default and object IDs/xref offsets are not stable across patch versions. A raw whole-file hash is non-deterministic run-to-run and brittle across pdf-lib patch bumps. The doc acknowledges '결정적 빌드' is needed but provides no mechanism (no setCreationDate/setProducer override, no font subset pin). The .brf/GPX goldens are fine (deterministic text), but the PDF SHA-256 goldens flake.
- Fix: For PDF channels replace the whole-file SHA-256 with a normalized hash: call pdfDoc.setCreationDate(FIXED)/setModificationDate(FIXED)/setProducer(FIXED) and pin a fixed font subset, OR hash only the extracted text-layer + field values. Keep the %PDF magic-byte and font-embed assertions. Document the determinism recipe in the golden-update procedure.

### F-22 [major] (lenses: research:legal; effort M)
**위치정보법 §1.1 vs §1.5 self-contradiction + missed 2024 소상공인 simplified-notification path: plan asserts it IS a 위치기반서비스사업자 while its own design persists no raw GPS — over-scopes the filing and leaves the classification undefended**
- Location: 11_legal_compliance.md:24-56 (heavy full-notification only), 85-104 + §1.1 (asserts 위치기반서비스사업자) vs §1.5 (raw GPS 비저장) + _research/D3_legal_a11y.md:153 (소상공인 exemption never propagated)
- Issue: Research verified the 2024 위치정보법 amendment created a 소상공인·1인 창조기업 path (exempt from prior notification for the first month; simplified notice thereafter). The canonical doc 11 describes only the heavy full-notification regime and a hard '신고 완료 before 8/31'. The exemption sits only in a research note. More fundamentally, MVP map-tap persists no raw GPS and does no continuous tracking, so the service may not even trigger 위치기반서비스사업자 classification — yet §1.1 asserts it does as flat fact, contradicting §1.5. A judge asking 'are you even a 위치기반서비스사업자 in the MVP?' has no defended answer.
- Fix: Two fixes, both keep the locked map-tap decision: (1) add the 2024 소상공인/1인-창조기업 simplified/deferred path and the team's eligibility determination; relax '신고 완료 before 8/31' to 'determine classification first, then file simplified or full as applicable'. (2) Resolve §1.1-vs-§1.5: state clearly whether MVP map-tap constitutes 위치기반서비스 at all and pre-write the Q&A answer.

### F-23 [major] (lenses: research:operating-account; effort S)
**Operating-account framing is overstated and miscoded: operating stage is 심의승인 (manual, NOT auto), 100,000/day is an advertised max requiring 활용사례 registration + a separate increase request (not an automatic grant), SLA 2-3일**
- Location: SPEC.md:102 + 04_kto_data_integration.md:506,538 + 01_overview_goals_scope.md:365 + 15_risks_open_items.md (R-D5, OI-6)
- Issue: Research verified on the KTO data.go.kr page: 운영단계 = 심의승인 (a KTO staffer must manually approve; can be queried/denied), SLA 2~3일, and 100,000/day is the platform max requiring (1) operating account + (2) 활용사례 registration (needs a live public URL) + (3) a separate traffic-increase request — two sequential 심의 round-trips, not one auto grant. The plan calls it 'auto' and assumes a flat 100,000/day. The demo is correctly insulated (INV-1, snapshot-only), so this is not demo-fatal, but the framing is wrong in a judge-facing way and could mislead pre-demo ETL scheduling.
- Fix: Reword SPEC/04/01/15: 'operating account -> traffic increase requested AFTER 활용사례 registration; 100,000/day is the advertised max, not an automatic grant; 운영단계 = 심의승인 (manual, 2-3일); budget two sequential 심의 round-trips.' Apply immediately after the first public deploy creates a registerable URL, target by Aug for a September buffer. Keep the snapshot-only demo insulation; add a pre-PT checklist item 'verify demo snapshot ingestedAt is recent' so lineage timestamps look current.

### F-24 [major] (lenses: devex, consistency; effort S)
**Workspace package scope flips between @modu / @modu-baekje / @domain across the frozen Domain Contract import line and architecture doc — half the SuitabilityResult import snippets won't resolve**
- Location: 12_workstreams_sequencing.md:74 + 13_testing_quality.md:125 (@modu/domain) vs 02_architecture.md:229,322 + 08:1236 (@modu-baekje) vs 05_feature_F1_total_guide.md:441 (@domain)
- Issue: The most-copied line in the blueprint imports the authoritative SuitabilityResult from @modu/domain/policy/types (docs 12, 13), but architecture/F4 use @modu-baekje and doc 05 uses @domain. Only one tsconfig path alias is real; the others are copy-paste errors that won't resolve. Every F1.A/F4/F5 consumer imports SuitabilityResult, and no doc declares THE workspace scope. A builder copying any snippet gets an unresolved module.
- Fix: Declare the workspace scope once (recommend @modu-baekje, matching the repo and the most detailed code) in 02 §2.1 + the C0-owned root package.json/tsconfig.base, normalize every embedded import across 02/05/08/12/13, and add it to the 00_README onboarding path.

### F-25 [major] (lenses: eng; effort S)
**Daily ETL revalidate emits tags (route:all, snapshot:latest) that don't exist in the cache-tag registry; route_guides/docent/dashboard read-models never actively invalidate**
- Location: 13_testing_quality.md:1457 (tags poi:all/route:all/snapshot:latest) vs 02_architecture.md:246-253 (registry: poi:all/poi:{id}/facts:all/guide:{poi}/docent:{poi}:{locale}/context:{area}/rto:dashboard) vs 04:404 (yet another set)
- Issue: The canonical kto-etl.yml workflow posts tags ['poi:all','route:all','snapshot:latest'], but the doc-02 registry defines no route:all or snapshot:latest tag, and doc 04 §6.3 lists a third set. After the nightly publish, route_guides/docent_assets/dashboard read-models are never actively invalidated — they refresh only on the 86400s TTL. A judge who watches the team push fresh route data the morning of the demo and sees it not appear disbelieves the pipeline. Same family as F-17 (tag-name drift) and F-05 (auth drift).
- Fix: Make the ETL emit exactly the registry tags: poi:all + each poi:{id} + guide:{poi} for changed guides + docent:{poi}:{locale} + rto:dashboard; context refresh emits context:{area}. Replace the hand-written tag list in 13 §8.2 with a call to the ETL's notifyRevalidate() so there is one source of truth, and add a test asserting every emitted tag is consumed by at least one unstable_cache factory and vice versa.

### F-26 [major] (lenses: eng; effort S)
**Odii xCoord/yCoord datum is never specified but stored as SRID 4326 and geofence-compared to WGS84 GPS — if Odii returns Korean projected coords the F2 docent geofence silently never fires**
- Location: 04_kto_data_integration.md:110,116,321 (xCoord/yCoord typed as bare string, datum unstated) vs 03_data_model.md:733 (docent_stories.trigger_point geography(Point,4326), 500m radius)
- Issue: doc 04 notes Odii's quirk that params are xCoord/yCoord (confirmed at line 110/116) but never states the coordinate system, and types them as bare string. doc 03 stores trigger_point as geography(Point,4326) and the F2 geofence compares to browser WGS84 GPS at 500m. KTO auxiliary endpoints frequently emit Korean projected coords (EPSG:5179/5186); the plan ships proj4js/pyproj precisely for this. If Odii returns projected coords stored raw into a 4326 column, ST_MakePoint treats 200000 as longitude, the point lands off Africa, every distance is >>500m, and the F2 docent (a core demo feature) silently never triggers — no error, since the column accepts the numbers.
- Fix: Add Odii datum verification to gate:0 GATE-5: probe one known POI, confirm whether xCoord/yCoord are WGS84 or projected, record the EPSG. If projected, route Odii coords through the existing pyproj transform before ST_SetSRID(...,4326). Add a normalization test asserting 부소산성's stored trigger_point is within ~1km of pois.geom (catches datum mistakes for all geo sources).

### F-27 [major] (lenses: eng; effort S)
**Multilingual contentTypeId 12/14 -> 76/78 remap is documented as required but absent from the getMultilingual client signature -> empty foreign-language captions and cards**
- Location: 04_kto_data_integration.md:169-171,326 (getMultilingual takes no contentTypeId, no remap) + SPEC.md:96 (EngService2 + 12 returns empty)
- Issue: doc 04 §2.8 and SPEC §96 warn the multilingual services need a different contentTypeId namespace (관광지 76 / 문화 78 / 행사 85) and that EngService2 with contentTypeId=12 returns EMPTY (research CONFIRMED the 76/78/85 scheme and the empty-response trap). The 6 POIs are type 12/14. But getMultilingual(p:{service;contentId}) takes no contentTypeId and there is no remap step in the ETL. An implementer wires it with the Korean type and silently gets empty title/overview for en/ja/zh-CN -> F2 4-language captions and foreigner F1.A cards render blank, undercutting the '외국인 4언어 횡단' selling point. Per-locale silent data loss.
- Fix: Define KTO_MULTILINGUAL_CONTENTTYPE_MAP ({12:76,14:78,15:85}) co-located with the field map, give getMultilingual a contentTypeId param (or look up pois.content_type_id and remap internally), document the normalize step. Wire the already-promised AC-9 (EngService2+12->empty) to assert the remap is applied (call uses 76/78, not 12/14).

### F-28 [major] (lenses: devex; effort M)
**Demo-resilience E2E aborts KTO at the browser, but the locked no-runtime-KTO architecture means the browser never calls KTO — the test is a no-op proving nothing about last-good-served**
- Location: 13_testing_quality.md:982-1000 (Playwright blocks apis.data.go.kr, asserts card renders) vs SPEC 2.7 + 02_architecture.md:347,160 (runtime never calls KTO; apps/web barred from kto-client)
- Issue: SPEC 2.7 and architecture lock that runtime requests never call KTO and apps/web cannot import kto-client. The demo-resilience spec blocks apis.data.go.kr at the Playwright browser layer and asserts the POI card still renders — but the page already never makes those calls, so the abort is a no-op; the test passes identically whether the snapshot last-good model works or is broken. It gives false confidence that 'demo survives API outage' is verified. The real risks (ETL publish-fail last-good-served, Kakao-map fallback) are never exercised.
- Fix: Replace the browser KTO abort with tests of the locked model: seed a Supabase state where the latest ETL batch failed and assert the prior published snapshot still serves; block the Kakao Maps SDK and assert list-only fallback renders with full accessibility info; keep a snapshot-mode test asserting zero external network calls.

### F-29 [major] (lenses: research:legal; effort S)
**F4 exported artifacts (학생 PDF / 쉬운글 PDF / BRF) carry AI-translated/narrated content but no in-document AI-source notice — AI 기본법 §31(2) is materially stricter for externally-distributed outputs than for on-screen badges**
- Location: 08_feature_F4_diary.md:11-22 (downloadable outputs) + 06_feature_F2_docent.md:630-641 (AI translation feeds F4) vs 11_legal_compliance.md:343-406 (badge story is on-screen only)
- Issue: Research (UNCERTAIN, leaning gap): per 과기정통부 guidance, when an AI output is downloaded/redistributed the output ITSELF must carry the AI-generation marking, not just a screen badge. F4 produces downloadable PDFs/BRF/GPX that can embed AI-translated docent text and AI-narrated transcripts, but the plan's badge/disclaimer story lives entirely in the on-screen player/card UI; nothing in doc 08 or 11 §4 specifies the exported files must bear an 'AI 번역/AI 음성 기반' notice. A 학생 PDF submitted to a school is exactly an external-distribution artifact.
- Fix: Add to F4 export builders and 11 §4: any exported artifact (PDF/BRF/HTML/GPX) containing AI-translated or AI-narrated-derived content must embed an AI-source notice in the document (footer line + machine-readable metadata where supported), mirroring the on-screen badge. Depth/compliance hardening on already-planned outputs.

### F-30 [major] (lenses: research:legal; effort S)
**AI 기본법 §31(1) advance-notification (사전 고지) is wholly uncovered — the plan implements only §31(2) per-output labels; no onboarding/ToS notice that the service operates on generative AI**
- Location: 11_legal_compliance.md:343-419,646-650 (only §31(2) badges; §31 appears as a bare section number)
- Issue: Research (CHANGED): §31 has TWO obligations — §31(1) advance notification that the product operates on generative AI (onboarding/ToS), and §31(2) per-output marking. The plan implements only (2) via AiBadge/AiDisclaimer. The §31(1) 사전 고지 is referenced nowhere operationally; the /privacy clause is a privacy notice, not the service-introduction notice §31(1) contemplates. A real, cheap-to-fix obligation currently uncovered.
- Fix: Add a §31(1) 사전 고지 surface: a one-time onboarding/ToS notice ('이 서비스는 생성형 AI(음성·번역·코스 추천)를 사용합니다') in the Accept-Gate flow (11 §9), plus a checklist item in 11 §10.3. Additive banner/ToS line, not a feature. Also add the clarifying line that the AI 기본법 obligation is effective NOW (the min-1-year window defers only 과태료/사실조사, not compliance) for the PT Q&A.

### F-31 [major] (lenses: devex; effort M)
**Domain test runner is declared vitest (C0-owned) but jest/jest-axe/jest.spyOn/jest.config are used throughout the testing doc with no named owner for a jest config**
- Location: 12_workstreams_sequencing.md:89,128,327 (C0 = vitest+playwright config) vs 13_testing_quality.md:544,622-642,1831-1833,1854 (jest.config.ts, jest.setup.ts, jest.spyOn, jest-axe, bare jest scripts)
- Issue: doc 12 sets the C0 domain gate as vitest and makes C0 owner of vitest+playwright config; jest is not in C0's owned list. But doc 13 uses jest.config.ts/jest.setup.ts, jest.spyOn, jest-axe, and root scripts call bare jest for contract/exports-golden/content-package suites. Those suites need a jest config with no named owner, while C0 owns a vitest config the testing doc never uses for them. A stream owner cannot tell whether to scaffold vitest, jest, or both, nor whom to ask for the missing config.
- Fix: Decide one runner per layer (vitest for packages/domain; jest for apps/web component+contract+exports — OR standardize on vitest everywhere with vitest-axe and vi.spyOn to match the C0 line). Add the chosen config files to a named owner in 12 §2.4 and fix the root scripts and jest references.

### F-32 [major] (lenses: research:pdf-braille; effort M)
**braillify wasm-pack ESM (synchronous import of index_bg.wasm) likely won't trace into the Vercel serverless/ETL bundle without explicit next.config WASM + outputFileTracingIncludes — never configured anywhere**
- Location: 08_feature_F4_diary.md:462,941 (braillify in nodejs API route) + 06_feature_F2_docent.md:287 (braillify in ETL) — no next.config WASM/tracing mentioned
- Issue: Research (UNCERTAIN): braillify 2.0.1 is ESM-only with import * as wasm from './index_bg.wasm' (classic wasm-pack bundler output). Per current Next.js/Vercel reports this is not traced into the serverless/route bundle by default — you must add experiments.asyncWebAssembly + a webpack rule AND outputFileTracingIncludes so index_bg.wasm ships with the function. The plan invokes braillify in an API Route (F4 export) and in ETL with zero WASM/tracing config anywhere.
- Fix: Add to F4 (doc 08 §7) and the F2 ETL: outputFileTracingIncludes for braillify's .wasm plus webpack.experiments.asyncWebAssembly=true (confirm Turbopack vs webpack). De-risk further by running braille generation in ETL/GitHub Actions, pre-storing .txt/.brf in Supabase Storage, and having F4 reuse the stored asset rather than calling WASM live in the export route. Flag as an open item with a build spike before the F4/BRF spike window.

### F-33 [major] (lenses: research:pdf-braille; effort S)
**react-pdf initKoreanFonts() registers fonts via a web-absolute path '/fonts/...' which throws ENOENT under renderToBuffer in a Node route — inconsistent with the same file's correct pdf-lib path; 3 of 6 channels render Latin tofu**
- Location: 08_feature_F4_diary.md:351-353 (src '/fonts/Pretendard-*.ttf') vs 08:293-296 (pdf-lib uses process.cwd() correctly), 397-401 (renderToBuffer)
- Issue: Research (CHANGED): under @react-pdf/renderer v4 renderToBuffer in a Node route (the plan's F4 export, runtime nodejs), a leading-slash/relative font src is passed to fontkit's fs.open and throws ENOENT (confirmed still failing 2025 on v4.1.5). The working pattern is path.join(process.cwd(),'public/fonts/...'). The SAME file's pdf-lib builder does it correctly with process.cwd(), so the react-pdf path (rubric/easy-read/group — 3 of 6 channels) is wrong while pdf-lib (student) is right.
- Fix: Change initKoreanFonts() to src: path.join(process.cwd(),'content/fonts/Pretendard-*.ttf') matching the pdf-lib location, and add that fonts dir to outputFileTracingIncludes so TTFs ship into the function. Add a golden/CI check that one react-pdf channel renders Korean (not just %PDF magic bytes) to catch the silent fallback-to-Latin tofu. (Combine with F-19 Hanja fallback and F-32 tracing.)

### F-34 [major] (lenses: design; effort M)
**Hero 4-Layer evidence card is spec'd as a formula dump (A×B×C×D weighted-rows table) — a judge cannot read 'why this score' in 5 seconds; the min() persona penalty (the smartest idea) is buried on one line**
- Location: 05_feature_F1_total_guide.md:295-326 (ScoreEvidenceCard ASCII A×B×C×D layout, [min personaFit] at 314) + 14_demo_pt.md:89-104 (샷1 money shot)
- Issue: This card is the product's core differentiator (데이터활용 20 + 기획력) and PT hero moment #1, but the only layout artifact is an engineer's worksheet: 'score = round(100 × A×B×C×D)', seven weighted rows, layer multipliers, evidenceConfidence, policyVersion. That is the right DATA but wrong HIERARCHY for the human question. The first thing a person needs is 'can MY group go, and what's the ONE thing to watch' — the math is the evidence, shown on demand. As specified the 5-second read is a multiplication table, and the min() penalty is buried as '[min personaFit]' on one line. The render rules correctly forbid color-only but establish no information PRIORITY.
- Fix: Re-spec ScoreEvidenceCard with a 3-tier hierarchy (keep all data, reorder): Tier 1 plain-language verdict + the ONE binding factor ('휠체어 진입은 가능하지만, 화장실 정보가 아직 없어요') driven by knownCriticalBlockers/deductions, not the number; Tier 2 the '왜?' chips (which persona constrained it via min(), confidence, freshness); Tier 3 the full A×B×C×D inside the existing details/summary drill-down. A re-spec, not new scope; lifts 기획력 and PT 적정성.

### F-35 [major] (lenses: office-hours; effort M)
**Demand is asserted from policy, never observed — no primary evidence (interviews/survey/n=) anywhere that the '5 questions 관광약자 face' are real; the entire 기획력/적정성 thesis rests on assertion**
- Location: base/proposal.txt:10-14 + 01_overview_goals_scope.md:64-78 + _research/ (no interview/survey artifact)
- Issue: The win-condition thesis is 'these 5 questions are what 관광약자 actually face.' A grep across the research corpus returns zero primary demand evidence — no interviews, n=, logged complaint, or baseline study. The only 관광약자 contact is July+ beta (a usability test of an already-built product, not demand discovery). A judge asking 'how do you know these are the real top-5 and not your guesses?' has no answer except the proposal's own assertion. The generous timeline is the calibrated place to buy this.
- Fix: Spend a cheap slice of runway on observed demand BEFORE the July build pass: 5-8 structured interviews with real 관광약자 (wheelchair, low-vision, parent of autistic child, 단체 인솔자) about their last heritage trip — what broke, would they use a pre-trip card. Capture 2-3 verbatim quotes per persona for the PT opener to replace the asserted '5 questions' slide with observed pain. Lifts 적정성 and gives the closing 실사용자 인용 real provenance. Pairs with F-36 (10x vignette) and F-37 (flywheel pilot).

### F-36 [major] (lenses: office-hours; effort S)
**10x-vs-status-quo is unproven: the plan shows richer information, never a 관광약자 reaching a different, better DECISION than calling ahead / 모두의여행 — richer != 10x**
- Location: 01_overview_goals_scope.md:68-78 (한계/응답 table) + 14_demo_pt.md:39-47 (problem slide), 422 (Q&A '단순 분류 아닌가')
- Issue: The status-quo contrast argues more transparency/dimensions, but a 10x improvement is measured in user OUTCOME, not feature richness. Today a wheelchair user calls ahead and gets yes/no; the plan never demonstrates the 4-Layer card causes a materially better trip decision (서문 over 동문, 대체추천 avoiding a wasted trip). The judge's own anticipated question '4단계 라벨만으로는 단순 분류 아닌가' is answered with 'it's a richer card' — a 10% answer.
- Fix: Add one concrete before/after decision vignette to the PT opener and doc 13: a 관광약자 who, with status quo, would have made decision X (wasted trip / stuck at 동문 stairs) and with 모두의백제 makes decision Y (서문 / 대체추천 박물관) — quantify the avoided harm. Frame the differentiator as 'avoids a failed/dangerous trip', not 'shows more fields'. Depth/validation, not new scope.

### F-37 [major] (lenses: office-hours, ceo; effort M)
**The F3->facts->F5 data-quality flywheel (발전성 20) is the core long-term claim but is structurally un-demonstrable in MVP (no auto-recalc, single UGC can't flip, anonymous-only) — asserted, never piloted once**
- Location: 01_overview_goals_scope.md:274 (사용자 늘수록 데이터 품질 순환) + SPEC.md:135 (F3 no-auto-recalc) + 16_suitability_policy.md:200 (single UGC can't flip)
- Issue: 발전성 (20pt) leans on '사용자가 늘수록 데이터 품질이 올라가는 순환'. But MVP correctly has no auto-recalc, single approved UGC can't flip authoritative status, and anonymous-only with no user pool — so the flywheel cannot spin in the demo; it is asserted as a design property. A judge can ask 'with 0 real users and manual moderation, what evidence is there this loop closes rather than stalls?' There is no piloted instance of one real report -> moderation -> fact refresh -> F5 gap delta on real data.
- Fix: Pilot ONE end-to-end real instance during the runway (not a seeded fixture): a beta tester files a genuine barrier report, run it through real moderation, show the verified_at refresh and the concrete F5 gap-count delta. Present THAT as observed evidence in the 발전성 slide, bounded to 'demonstrated once, scales with users'. Keeps the no-auto-recalc safety decision intact while making 발전성 observed not asserted.

### F-38 [major] (lenses: consistency; effort S)
**Two embedded CREATE TYPE blocks disagree (absence_reason/'unentered' in doc 05 vs absence_kind/'operator_missing' in doc 03); building from doc 05 makes the F5 gap MV silently return 0 missing-operator counts**
- Location: 05_feature_F1_total_guide.md:44-48,55 (CREATE TYPE absence_reason, value 'unentered') vs 03_data_model.md:48,408,1175 (absence_kind, value 'operator_missing', F5 MV filters operator_missing)
- Issue: doc 05 ships CREATE TYPE absence_reason ENUM ('intrinsic','unentered','not_applicable') and accessibility_facts.absence_reason. doc 03 (the DB-contract owner) ships absence_kind ENUM ('intrinsic','operator_missing','not_applicable') and the F5 completeness MV filters absence_kind='operator_missing'. Different type name AND value name for the same '운영자 미입력' concept. Building from doc 05 makes the F5 gap MV return 0 missing-operator counts (the whole F5 gap-report breaks) and the column the domain reads doesn't exist.
- Fix: doc 03 is canonical. Change doc 05 §1.1 to absence_kind with value operator_missing, or replace the doc-05 DDL with a link to doc 03 §1.2/§3.5.

### F-39 [major] (lenses: consistency; effort S)
**docent channel definition disagrees: enum ('audio','caption','braille','sign') in doc 03 vs text CHECK ('audio','transcript','braille','sign_video') in doc 06 — F2 4-channel matrix (a PT hero) can't be seeded consistently**
- Location: 03_data_model.md:78,748 (docent_channel enum) vs 06_feature_F2_docent.md:88 (channel text CHECK with transcript/sign_video)
- Issue: doc 03 (DB owner) defines docent_channel enum ('audio','caption','braille','sign'); doc 06 defines the same column as text CHECK ('audio','transcript','braille','sign_video'). Two of four values differ (caption vs transcript, sign vs sign_video) and the mechanism differs (enum vs text+CHECK). Any insert against one set fails the other's constraint; the F2 4-channel matrix cannot be seeded consistently.
- Fix: Make doc 06 reference doc 03's docent_channel enum verbatim and drop its text+CHECK. If 'sign_video' encodes an intentional video-vs-still distinction, put it in a separate media-kind column, not a divergent channel value.

### F-40 [major] (lenses: consistency; effort M)
**PersonaId domain is undefined and used three incompatible ways (['wheelchair','senior','family'] vs ['P1a','P3'] vs string[]) and matches neither the DB persona_code enum — domain and DB cannot round-trip persona identity; golden tests keyed P1a won't match a card sending 'wheelchair'**
- Location: 05_feature_F1_total_guide.md:458,124 (PersonaId 'wheelchair'/'low_vision') + 13_testing_quality.md:212-213 (P1a/P3) + 08_feature_F4_diary.md:100 (string[]) + 03_data_model.md:64 (persona_code enum wheelchair/senior/visual/hearing/family/group_leader/cognitive)
- Issue: PersonaId is the canonical persona type but its value set is undefined: doc 05 uses 'wheelchair'/'low_vision'/'stroller'/'low_stimulus', doc 13 uses 'P1a'/'P3', doc 08 declares string[], and the DB enum uses a different set again (none of the Pxx codes, none of 'low_vision'/'low_stimulus'). The domain and DB cannot round-trip persona identity; golden tests keyed to P1a won't match a card sending 'wheelchair', and the UI can select a persona the engine can't score.
- Fix: Define PersonaId once (align to the DB persona_code enum OR a fixed Pxx<->enum bidirectional map in one place). Replace doc 08's string[] with PersonaId[], fix doc 05's stray values, and make doc 13 tests use the same literals as the doc 16 §4.1 matrix (P1a..P4). Mirror the SuitabilityResult single-source pattern.

### F-41 [major] (lenses: consistency; effort M)
**doc 13 boundary tests use withScore()/withCoverage() helpers that inject OUTPUTS the real engine produces — the '<70 alternatives' / '74/75 label' criteria are asserted against fabricated numbers, never exercised on real A·B·C·D math**
- Location: 13_testing_quality.md:238-267,269-278 (calculateSuitability(withScore(69)) etc.) vs 05_feature_F1_total_guide.md (CalculateSuitabilityInput takes facts/personas/certs, not score)
- Issue: doc 16 §10 boundary requirements (alternatives at <70, label 74/75, coverage 0.64/0.65) are 'tested' via withScore(69)/withScore(70)/withCoverage(0.64). score and coverage are OUTPUTS of calculateSuitability, not inputs — there is no documented way to force an exact output from the real input shape. These helpers imply a mock/forced engine, so the acceptance criteria are asserted against fixture-fabricated numbers, not the production formula. A judge probing reproducibility could find the boundary never exercised on real math. (Same anti-hand-math discipline F1 §4.5 already mandates.)
- Fix: Replace withScore/withCoverage with real fact fixtures hand-tuned so the engine PRODUCES 69/70/74/75 and coverage 0.64/0.65 (named golden cases), or document withScore() as a thin input-builder (not a score injector). Tie each boundary test to a committed {input,expected} golden so the boundary is proven on the actual formula.

### F-42 [major] (lenses: research:supabase; effort M)
**F5 priority-engine worked-example numbers are hand-authored and one (공산성 elevator=0.02) doesn't satisfy the engine's own factor mapping (actual ~0.006) — F1 already bans hand-maintained numbers; F5 doesn't, so the headline table a judge reads carries a wrong number**
- Location: 09_feature_F5_rto_dashboard.md:298-299,511-519,607-611 (0.02 vs computed 0.5×0.2×0.6×0.1=0.006) vs 05_feature_F1_total_guide.md:495 (F1 mandates golden-generated numbers)
- Issue: Research (UNCERTAIN, verified arithmetic): doc 09 shows 공산성 elevator priority=0.02 ('본질 제약'), but per the engine (unsupported -> severity 0.2, feasibility 0.1, impact 0.5, confidence 0.6) the product is 0.006, rounding to 0.01 not 0.02. The 부소산성=1.00 case checks out, but the 0.02 case doesn't match the stated mapping. F5's numbers are hand-authored (unlike F1 which mandates golden-generated), so they drift from the engine silently. A judge or teammate recomputing 0.02 gets a different number.
- Fix: Apply F1's anti-hand-math discipline to F5: generate the doc 09 §2.6/§6.2 example numbers from a rankPoiByGapPriority golden fixture and inject them; re-derive 공산성 (likely ~0.01) once the factor mapping is final; add a golden test for the priority engine paralleling the 30 suitability cases.

### F-43 [minor] (lenses: research:supabase; effort S)
**F5 PT-stable snapshot (pt_demo_2026_10) is built but the page hardcodes getRtoDashboard('live') with only a code-comment switch — flipping to the stable snapshot for the demo requires a redeploy on demo day**
- Location: 09_feature_F5_rto_dashboard.md:789-794 (hardcoded 'live', comment 'PT 기간엔 pt_demo 사용'), 891-894 (AC-F5-05), 276 (live overwritten by ETL)
- Issue: The plan builds a PT-stable seed snapshot specifically so the live demo is immune to ETL drift (good), but the RSC page hardcodes getRtoDashboard('live') and the only switch is a code comment. Flipping to the stable snapshot requires a code edit + redeploy on demo day — exactly when you don't want to touch the build. The point of the seed (SPEC §13.8) is defeated if the F5 hero still reads 'live' which ETL overwrites nightly.
- Fix: Make the label selectable without redeploy: read from env (RTO_SNAPSHOT_LABEL, default 'live') or a route/query param so the demo URL is /rto?snapshot=pt_demo_2026_10. Add to the demo runbook and AC-F5-05. Cheap insurance for SPEC §13.8.

### F-44 [minor] (lenses: research:supabase, consistency; effort S)
**Cut '2박3일' tier still lives in the F4 TimeMode union and doc 01 prose describes the cut 6단 builder in present tense — F4 type admits an impossible state, doc 01 contradicts its own scope table**
- Location: 08_feature_F4_diary.md:101,145 (TimeMode includes 2박3일) + 01_overview_goals_scope.md:66,73 (present-tense 6단/2박3일) vs SPEC.md:178 (§13.2 MVP=3단) + 01:128,142 (correctly marks 발전방향)
- Issue: SPEC §13.2 cut 시간예산 to MVP 3단 (반나절/당일/1박2일). doc 05 was updated, but doc 08 still defines TimeMode = 반나절|당일|1박2일|2박3일 and uses it in DiaryEntry.timeMode — a DiaryEntry persisted with timeMode='2박3일' is a state the cut planner can never produce yet F4 export/PDF would accept. doc 01 §2.1 prose still describes the 6단/2박3일 builder in present tense, contradicting doc 01's own scope table. RC-3 fixed doc 12 but missed doc 08's TYPE and doc 01's prose.
- Fix: Change doc 08 TimeMode to 반나절|당일|1박2일 (and the comment), using a separate FutureTimeMode if forward-compat is wanted. Fix doc 01 line 66/73 to '반나절~1박2일 (MVP 3단; 6단=확장 상한)'. Both a build-correctness fix (type admits impossible state) and a credibility fix (doc 01 opening contradicts its scope).

### F-45 [minor] (lenses: consistency; effort S)
**Both SPEC.md and SPEC.ko.md declare themselves THE single authority with no deference clause — a Korean-reading judge/teammate treats SPEC.ko.md as canonical, and any numeric drift between them has no defined winner**
- Location: SPEC.md:3 ('single authoritative spec') vs SPEC.ko.md:3 ('이 파일이 유일한 권위 SPEC이며')
- Issue: Both files claim to be the single authority; neither states the project rule that SPEC.ko.md is a translation deferring to SPEC.md. Both carry full §13 bodies, doubling the divergence surface. A future numeric edit landing in only one has no defined winner.
- Fix: Edit SPEC.ko.md:3 to state 'SPEC.md가 단일 정본이며 본 한국어 문서는 번역본으로서 충돌 시 SPEC.md를 따른다.' Optionally add a CI check flagging SPEC.md changes without a matching SPEC.ko.md update.

### F-46 [minor] (lenses: consistency; effort S)
**F2 sign-language scope contradicts SPEC §13.2: doc 03 note says '수어 6 POI 샘플' while SPEC/doc 06 lock sign video to 공산성·부소산성 only — undermines the 'we held scope' narrative**
- Location: 03_data_model.md:760 (수어 6 POI 샘플) vs SPEC.md:178 + 06_feature_F2_docent.md:31,414,683 (sign video only 공산성·부소산성)
- Issue: SPEC §13.2 locks '4채널·수어 deep only for 공산성·부소산성' and doc 06 AC-F2-07 enforces sign video only for those two. But doc 03 §4.6 note says '수어 영상은 6 POI 샘플(MVP)'. A reviewer comparing the data-model note to SPEC sees the cut not applied, undermining the depth-over-scope narrative.
- Fix: Change doc 03 §4.6 note to '수어 영상은 공산성·부소산성 데모 페어만(MVP), 나머지 4 POI는 음성+자막 (SPEC §13.2)'. Keep the matrix as schema ceiling but annotate which cells are MVP-populated.

### F-47 [minor] (lenses: eng; effort S)
**verifiedAt nullability contract conflict: domain says string|null (needed for unknown facts) but tests/policy type it non-null — unknown facts can't be represented; surfaces the moment the 30 golden cases are authored**
- Location: 02_architecture.md:212 (verifiedAt: string|null) vs 13_testing_quality.md:105 (verifiedAt: string) vs 16_suitability_policy.md:46 (dataDates verifiedAt: string)
- Issue: doc 02's PoiFact types verifiedAt: string|null ('null => 미검증'), essential because an unknown/operator-missing capability has no verification date. But doc 13's AccessibilityFact test-input declares verifiedAt: string (non-null) and SuitabilityResult.dataDates is non-null. Half the golden cases (all-unknown, buyeo-unknown-coverage-low) require exactly that, so either fixtures fabricate verifiedAt for unknown facts (polluting Layer-D freshness) or the type rejects valid inputs.
- Fix: Unify on verifiedAt: string|null in the authoritative types and propagate to doc 13; make SuitabilityResult.dataDates include only capabilities that have a verifiedAt (filter nulls). Add a golden case asserting an unknown fact carries verifiedAt=null and is excluded from Layer-D averaging.

### F-48 [minor] (lenses: eng; effort M)
**publish_dataset() copies staging row id verbatim and inserts without ON CONFLICT against the natural key — cross-poi id collision aborts the publish, and unstable ids cascade-delete accessibility_evidence on every republish**
- Location: 03_data_model.md:223-228,416 (insert ... select id ... ; unique(poi_id,capability_code,source); accessibility_evidence.fact_id FK on delete cascade)
- Issue: publish_dataset() does insert into accessibility_facts select id, ... from staging after delete-by-poi_id. Two hazards: (1) if staging regenerates fresh uuid ids each batch, every publish changes the surrogate id, breaking accessibility_evidence.fact_id (FK on delete cascade) — the delete step cascade-deletes evidence packs on every publish unless ids are stable; (2) the insert carries id rather than ON CONFLICT against unique(poi_id,capability_code,source), so a staging id colliding with a surviving live id (a poi not in the delete set) raises a PK violation aborting the whole publish.
- Fix: ON CONFLICT against the natural key (poi_id,capability_code,source); either derive stable ids deterministically from the natural key so accessibility_evidence.fact_id survives, or let the live table keep its id and upsert by natural key. Add an etl test republishing an unchanged batch asserting accessibility_facts.id and dependent evidence rows are unchanged.

### F-49 [minor] (lenses: research:next-cache, research:supabase; effort S)
**Multiple silent-timeout/build traps: vercel.json functions glob may not match in the monorepo (export functions fall to default timeout); '`use cache`' guard is wrong (build error, not no-op); Vercel Pro is a HARD prerequisite for icn1 Seoul residency, not a convenience**
- Location: 02_architecture.md:307-308 (functions glob src/app/api/exports/** relative to project root), 288 ('use cache no-op') + 02:338,598 + 01:371 (Vercel Pro framed as convenience)
- Issue: Three small build/demo traps. (1) vercel.json functions globs resolve relative to the Vercel Project Root Directory; if root is the repo root not apps/web, the export glob matches nothing and PDF/BRF functions silently fall back to the default duration (timeout on long generation), no build error. (2) doc 02:288 claims '`use cache` is a no-op when cacheComponents off' — research verified it is a hard BUILD ERROR, so the guard wording is wrong (and a stronger guard than stated). (3) Research verified icn1 (Seoul) function deployment is Pro-only — Vercel Pro is a HARD prerequisite for the locked Seoul data-residency claim (doc 11), not a cron convenience.
- Fix: (1) Note the functions glob is relative to the Vercel root which MUST be apps/web (or prefix the glob); add to the RC/deploy checklist to prevent a silent demo-day export timeout. (2) Reword 02:288 to 'do not introduce `use cache` — it is a build error while cacheComponents is off.' (3) Elevate 'Vercel Pro' to a launch-blocking prerequisite tied to the icn1/PII posture, and run context-refresh testing on a Pro env (Hobby blocks sub-daily cron).

### F-50 [minor] (lenses: research:kwcag; effort S)
**Cert-application tooling references are outdated (K-WAH is a KWCAG-2.0-era tool, mis-claimed as a 33-item KWCAG-2.2 필수 서류) and F5 cites a KWCAG-non-adopted item (§2.4.11) as a compliance gate**
- Location: 10_accessibility_kwcag.md:146,962-969,1002 (K-WAH 33 검사항목 필수 서류) + 09_feature_F5_rto_dashboard.md:752 (§2.4.11 focus) vs 10:88 (2.4.11 미채택)
- Issue: Research (CHANGED): K-WAH checks KWCAG 2.0's 6 지침, is KWCAG-2.0-era/deprecated, and does not produce a 33-item KWCAG-2.2 report; framing it as the 인증-신청 필수 서류 is overstated. Current self-check ecosystem is OpenWAX/NWAX + the KWACC/WA KWCAG-2.2 표준 점검 지침. Separately F5 line 752 cites §2.4.11 (Focus Not Obscured) as a compliance row, but doc 10 line 88 lists it as KWCAG 미채택 (voluntary) — the same numbering-credibility drift as F-15. A 심사원 would catch both.
- Fix: Replace K-WAH references with OpenWAX (automated) + the KWCAG 2.2 표준 점검 지침 manual checklist as the self-assessment of record; reframe the deliverable as 'KWCAG 2.2 자체점검 결과서 (지침 기반)'. Fix F5 line 752 to drop §2.4.11 (or cite the adopted 2.1.2). Also add the WA 30%/40% non-profit/social-enterprise discount to §8.3 (cost ~770k-1.05M), and state affirmatively that self-assessment uses the SAME KWCAG 2.2 standard the formal cert evaluates against (a PT credibility win).

### F-51 [minor] (lenses: research:kwcag; effort S)
**axe-core wcag22aa gate is contradictory across the two CI-authority docs (PR-blocking in doc 13 vs warn-only in doc 10), and the 9 KWCAG-2.2-NEW mandatory items (several not axe-detectable) have NO enforced merge gate**
- Location: 13_testing_quality.md:914-918,1080 (wcag22aa PR 차단) vs 10_accessibility_kwcag.md:132-140 (wcag22aa warn->merge 허용); storybook config drops wcag22aa
- Issue: Direct contradiction: doc 13 makes wcag22aa PR-blocking; doc 10 §3.1 tiers it as warn-only. Worse, the KWCAG-2.2-NEW mandatory items (2.1.4, 2.5.1, 2.5.2, 2.5.3, 2.5.4, 3.3.3, 3.3.4, 2.4.4) — the ones that distinguish KWCAG 2.2 from 2.1 — are several not axe-detectable at all, and the storybook config drops wcag22aa, so they have NO enforced gate, only a manual checklist that is not a merge gate. A judge auditing 'KWCAG 2.2 준수' probes exactly these 9.
- Fix: Reconcile to ONE wcag22aa policy (recommend: axe wcag22aa as warn since incomplete, but make the manual checklist for the 9 KWCAG-2.2-new SCs an explicit RC-blocking gate with per-item evidence). Add a mapping table 'KWCAG-2.2-new item -> automatable? -> which gate covers it' so coverage gaps are visible. Fix doc 13 lines 914/918/1080 or doc 10 §3.1 so they match.

### F-52 [minor] (lenses: devex, consistency; effort S)
**Exports renderer API + content-package layout + POI slug are inconsistent across the docs a builder reads in sequence (4 export fn names, gongsanseong vs gongsan vs gongsan-264736, flat-file vs dir-per-POI) — blocks the C4 contract test and F1-E/F4 import hand-off**
- Location: 12_workstreams_sequencing.md:117,31 vs 02_architecture.md:134 vs 08:1141 vs 13_testing_quality.md:326-346,582,402,828 vs 02_architecture.md:265
- Issue: (a) The CX Exports contract is named four ways (exportGpx/renderPdf/renderBrf/renderHtml vs renderStudentPdf/fillChungnamForm/toBrf/toGpx vs buildStudentPdf vs buildDiaryDocument(input,'student-pdf')) — F1-E and F4 can't write a correct import. (b) Content package is named flat (content/pois/gongsanseong.json) vs dir-per-POI (content/pois/gongsan/package.json); slug varies gongsanseong/gongsan/264736; cache tag poi:gongsanseong vs data-testid poi-gongsan. A C4 owner can't tell the layout, and the unstable slug propagates into revalidateTag tests and slice fixtures on the Window-1 path.
- Fix: Freeze one export API in packages/exports/src/index.ts and link every doc to it (reconcile the GPX entry with F1-E/F4). Fix one canonical slug (gongsanseong) and one layout (dir-per-POI) in the Content Package Contract row, and propagate to doc 13, cache tags, and data-testids. Add the slug list to the C4 DoD.

### F-53 [minor] (lenses: design; effort L)
**No design language / visual system / mascot-voice spec — high AI-slop risk for a product judged on 완성도-30; raw hex already leaking into PDF builders**
- Location: 10_accessibility_kwcag.md:511-563 (ContrastTokens only) + 08_feature_F4_diary.md:569,792-797 (hardcoded #F0F7FF, borderRadius:8, inline CSS) + SPEC.md:142 ('Design Contract v1' never expanded)
- Issue: The plan has a thorough accessibility token set but no DESIGN system: no type scale, spacing/radius/elevation, iconography stance, layout grid, illustration/mascot voice, or motion language. The 백제 mascot (the only custom asset, the emotional anchor of cognitive-friendly mode and kid diary) has no character/tone brief. Ad-hoc values already leak (08:569 hardcoded hex/radius in a PDF builder). With React+Tailwind and no opinionated direction, the default is generic 'AI dashboard' aesthetics — which a 완성도-30/발표-15 judge reads as a template.
- Fix: Expand 'Design Contract v1' into a real design-system spec (extend doc 10 or add a doc): type scale + spacing/radius tokens shared by PDF and web (killing the 08:569 hardcoding), a 백제-heritage aesthetic direction, an icon/pictogram stance reconciling KS/ARASAAC sets, a motion language tied to prefers-reduced-motion, and a one-page mascot character + voice brief reused by F1.F/kid diary/empty states. Depth/polish, exactly the locked calibration.

### F-54 [minor] (lenses: design; effort M)
**예측 가능 백제 (cognitive-friendly mode) — a flagship inclusion claim and PT D.5 moment — is underspecified at the experiential level: component names only, no calm/단계 visual design**
- Location: 05_feature_F1_total_guide.md:590-615 (F1.F component list) + 06_feature_F2_docent.md:57 (쉬운글) + 14_demo_pt.md:263-277 (D.5 자폐 scenario)
- Issue: F1.F is positioned as a flagship inclusion story (PT D.5) and the KWCAG 3.2 예측가능성 hook, but the spec is component names + data sources only (SingleStepCard '이전 단계 숨김', SensoryCalmSlider '4축'). There is no cognitive-friendly DESIGN — no spec for how now/next-only reduces load visually, no calm-mode visual transformation, no consistent-prediction layout, no transition/anticipation pattern, no 7세-어휘 copy guidelines. For the population this serves, the visual/temporal design IS the feature; a list of toggles is not.
- Fix: Add an experiential spec to doc 05 §6.2 (align doc 10 motion rules): the now/next visual model (shown/hidden/preview), the calm-mode visual delta (BGM off + reduced stimulus-contrast + larger targets + zeroed motion), a 7세-어휘 copy guideline with do/don't (reused with docent 쉬운글), and a predictable-layout rule (same elements, same place every step). Deepens locked scope, no new scope.

### F-55 [minor] (lenses: design; effort M)
**No first-5-sec / 5-min / 5-year arc — no designed home, no no-login first-run path (despite no-login being core); a cold judge lands inside a feature with persona/jargon assumptions already made**
- Location: 14_demo_pt.md:78 (only '첫 화면' ref is a contrast-overlay) + SPEC.md:32,52 (no-login core, never designed) + absence across 05/06/08/09
- Issue: Every feature doc starts at the feature; nothing designs the ARRIVAL. The only '첫 화면' reference is a contrast-ratio debug overlay, not a user's first 5 seconds. No home/landing IA, no no-login first-run path (despite SPEC making no-login core), no 'what is this / who / where do I start' moment, no returning-user re-entry that uses the local profile, no repeat-visitor/diary continuity surface (the '3번째 방문' badge is mentioned once with no home). For a judge doing a cold open without the narrated PT, the app has no front door.
- Fix: Add a 'First-run & Home IA' spec: the no-login landing (purpose in one line + warm persona-template entry from F-13 + a single obvious first action + F1->F5 value at a glance); the returning-user re-entry applying the local profile with a visible 'OO님으로 보고 있어요 (변경)' affordance; and the repeat-visitor/diary continuity surface. The connective tissue the feature catalog is missing.

### F-56 [improvement] (lenses: ceo; effort S)
**Diary->'다음 방문자 환류 검증 코스' over-claims a community loop: the emitted GPX is the curated F1.B route (re-distribution), not a user-generated verified course (UGC GPX was cut to 발전방향)**
- Location: 14_demo_pt.md:176 (샷5-f), 404 (추적 포인트 3) + 08_feature_F4_diary.md:12-19,108 + 01_overview_goals_scope.md:143 (F1.E UGC GPX 발전방향)
- Issue: PT key message #3 says the diary GPX = '다음 방문자가 그대로 따라갈 검증된 코스로 환류'. But UGC GPX was cut (F1.E -> 발전방향) and doc 08 confirms the diary GPX is the F1.B curated route — the user re-downloads the same team-authored route, not a course they verified and contributed back. The 'circulates to the next visitor' framing implies a community feedback loop the MVP deliberately lacks. A judge probing '사용자가 만든 코스가 다음 사용자에게 가나?' surfaces re-distribution, not circulation. A small over-claim sitting on one of only three things the judge is asked to remember.
- Fix: Tighten to what is true: 'a verified barrier-free course travels WITH the visitor and to the next visitor (same curated, expert-verified route), with the community-contributed loop on the 발전방향 roadmap.' Reserve '환류/circulation' for F3 (report->moderation->next visitor's alert), which IS a closed loop; let F4 own 'portable multi-format output (GPX/BRF/PDF)'.

### F-57 [improvement] (lenses: design; effort S)
**F4 6-output 'money shot' produces a silent ZIP — the most persuasive thing (one record fanning out to 6 human audiences) happens off-screen; no output-selection design**
- Location: 08_feature_F4_diary.md:1015-1021 (checkbox ChannelSelector + ZIP), 982-993 + 14_demo_pt.md:166-185 (샷5 실용성-25 hero)
- Issue: The 6-output diary is the PT 실용성-25 money shot, but the export UX is a checkbox ChannelSelector producing a .zip and the PT leans on 'ZIP 1회 다운로드'. A silent ZIP is the least legible way to land 'the same data became 6 things for 6 different people' — the judge sees a spinner, not the transformation. Each output has a distinct human purpose (학교 제출/교사 채점/시각장애인/인지장애인/다음 방문자 GPX/단체) flattened into equal jargon peers.
- Fix: Re-spec ExportPanel so each channel is a labeled OUTCOME card with audience + live preview/thumbnail ('학교에 제출할 PDF', '시각장애인이 읽을 점자', '다음 방문자가 따라갈 지도'), and make the demo open 1-2 outputs visibly (judge SEES the diary become a 충남교육청 form and a .brf). Keep the ZIP as convenience, not the hero. Surfaces the lineage thesis at the pixel level for almost no build cost.

### F-58 [improvement] (lenses: design; effort S)
**F5 closing 'proof' screen is a bare table with no visual tie-back from a gap row to the F1 card / F3 report that produced it — the loop is asserted in narration, not shown**
- Location: 09_feature_F5_rto_dashboard.md:587-612 (GapPriorityTable only) + 14_demo_pt.md:188-203 (샷6, lineage close)
- Issue: F5 closes the F1->F5 argument live (PT hero #3). The scope cut to a single gap-priority table is correct, but the design is now ONLY an accessible table of priority scores. It is honest and a11y-correct, but as the visual climax of 'the same data came all the way around' it doesn't make the LOOP legible — no visual tie-back from a F5 gap row to the F1 card/F3 report. The judge takes the closed-loop claim on narration; the screen doesn't show it.
- Fix: Without adding heatmaps (stay within the cut), add a lightweight lineage affordance to the top gap row: a 'where this came from' inline trace ('공산성 elevator: F1 카드 정보없음 -> F3 제보 2건 -> 여기') and one sentence framing the screen as the loop closing. Copy + small-component decision, not restored scope; converts the closing table into visible proof. (Backs F-12's lineage exhibit.)

### F-59 [improvement] (lenses: office-hours; effort S)
**PT shot 2-d narrates the 만하루 6deg slope detour as benign for the wheelchair할아버지, but the plan's own research flags >8.3% as a barrier — turn the honest constraint into the differentiator**
- Location: 14_demo_pt.md:118 (단계 카드 4: 만하루 6deg 우회로 as positive) + 05_feature_F1_total_guide.md:351 + _research R5 (>8.3% = barrier)
- Issue: Shot 2-d narrates '만하루 진입로 우회 (경사 6deg 우회로 -> 북쪽 평탄 경로)' as a positive. For a real 70대 wheelchair할아버지, 6deg (~10.5%) exceeds the BF ramp threshold (R5 flags >8.3% as a barrier) — presenting this as a smooth part of his half-day is exactly what an accessibility-literate judge catches as a credibility gap. The plan treats 6deg as benign while its own research classifies it as a barrier.
- Fix: Reframe shot 2-d honestly: have the 4-Layer card flag the 6deg segment as 'partial / 주의 — 보조자 동반 권장' with the measured grade visible, and route around it or recommend the flat 박물관 alternative for the unaccompanied case. Makes 'transparent evidence card' a demonstrated promise and converts a hidden weakness into the strongest 정직성 moment. Pairs with F-08 (verify-before-seed).

### F-60 [improvement] (lenses: design, eng; effort M)
**Trust/provenance data exists (confidence chip, AI badge, KOGL attribution, license footers) but as scattered per-feature elements with no unified visual trust language; AND the offline fallback_text NOT-NULL invariant is prose-only, enforced by no DB/CI guard**
- Location: 16:185 + 06:645-667 + 05:243 + 08:845-883 (scattered trust elements) + 03_data_model.md:684,720,1285 (assets jsonb, fallback_text invariant prose-only)
- Issue: Two improvements bundled. (a) The plan has unusually good trust DATA (per-axis evidenceConfidence, data dates, AI badges, KOGL/cpyrhtDivCd attribution, license footers) but each lives in its own doc as an ad-hoc element — no unified trust visual language across F1/F2/F4/F5. A barrier-free product lives or dies on whether a disabled user believes the data; confidence/freshness/source/AI-gen/verification should read as ONE coherent system. (b) The offline-degradation guarantee (M-19) hinges on every assets[] entry having non-empty fallback_text, but assets is a single jsonb blob with the per-entry shape in a markdown comment only; the only enforcement is an AC an implementer must remember to script — a blank-fallback bundle publishes silently and renders a blank offline step (visible only offline).
- Fix: (a) Define a single 'trust & provenance' visual primitive set in the design contract: one chip family (confidence/freshness), one badge family (AI-gen), one attribution pattern, shared tokens + aria, applied identically everywhere. (b) Promote the fallback_text invariant from prose to an enforced CHECK/trigger that rejects an offline_bundle_manifests insert with any entry missing fallback_text, keep the AC as a second line, and add an offline E2E aborting the asset URL asserting fallback_text renders for every step.

### F-61 [improvement] (lenses: research:next-cache, research:supabase, research:operating-account; effort S)
**Several CONFIRMED facts need provenance/label tightening so a probing judge can't find an overstatement: TatsCnctr is a 30-day FORECAST (not current crowding), Supabase wrong-region create silently breaks 국외이전, GH-Actions DB-ping must be a real query, stale areaCode cutoff claim lingers in research**
- Location: 04:125 (혼잡도 label) + research:supabase (ap-northeast-2 immutable) + R-D8 (DB-ping) + _research/D1_kto_api.md:115,364 (stale 2025-12-31 cutoff)
- Issue: Cluster of CONFIRMED-but-needs-tightening items: (1) TatsCnctrRateService is a 30-day-forward ML FORECAST, but doc 04:125 labels it '혼잡도(상대)' implying a current/historical measurement — a judge probing 'is this real-time crowd or a model guess?' finds the framing overstates certainty. (2) Supabase region is immutable after create; a wrong-region create silently violates the locked 국외이전 posture and needs a full re-provision. (3) The R-D8 keep-alive cron must issue a real authenticated SELECT (a dashboard visit or paused-project ping does NOT reset the 7-day clock). (4) The stale '2025-12-31 areaCode cutoff' claim still lives in _research/D1 and could mislead an implementer (canonical doc 04 is correct: rolling 90-day per-service, no global date).
- Fix: (1) Relabel to '예측 혼잡도(향후 30일, KT 이동통신 ML 추정)' and add an effective_period + 'predicted' provenance flag on context_snapshots.crowd. (2) Add a provisioning checklist item 'create project in Northeast Asia (Seoul) ap-northeast-2 explicitly'. (3) Specify the keep-alive cron as an authenticated SELECT against a real table. (4) Strike the stale 2025-12-31 cutoff in _research/D1 and add a CI/grep guard that no source emits areaCode/sigunguCode/cat* as a write filter.

### F-62 [improvement] (lenses: research:maps-heritage; effort S)
**Kakao/juso/VWorld cost+access facts are correct in research but never propagated to canonical docs, and two have no scheduled owner: no Kakao quota/cost note anywhere, juso 좌표제공 approval is unscheduled, VWorld DEM security-restriction not flagged at the source list**
- Location: 01_overview_goals_scope.md:182-196,187,188 + 04/02/11 (no Kakao quota) + 12/15 (juso approval not in open-items)
- Issue: Research CONFIRMED four maps/geo facts but they live only in R5, not the canonical docs: (1) No Kakao quota/overage budget statement anywhere in 01/02/04/11 — a judge auditing cost/scalability has nothing to read (free 300k Map/100k Local daily; overage 0.1/0.5/2 KRW; the 80% discount is Talk-Share-only, irrelevant to maps). (2) juso 좌표제공 API requires 본인인증 + approval but appears only in a source table, NOT in the workstream lead-time/open-items list (unlike the KTO operating account) — no owner/date. (3) VWorld 3D/DEM is security-restricted (공개제한, no overseas use) but 01:187 lists 'VWorld' as a space source without flagging it must NOT be the slope source (NGII open DEM is). (4) Kakao Mobility walking-directions is partner-gated and intentionally OUT — should be stated once so the static-route choice doesn't read as a capability gap.
- Fix: (1) Add a 2-3 line Kakao cost note + a geocoding-cache rule (cache address<->coord to stay under 100k/day Local). (2) Add juso 좌표제공 approval as a tracked early-apply open item with an owner/date, and route all juso calls through ETL-time only with the mandatory SQL/special-char input filter. (3) Tighten 01:187 to scope VWorld to free 2D tiles/geocoder/admin-boundary only and name NGII open DEM as the slope source in the source list (currently only in derived-column comments). (4) State once that Kakao Mobility walking-directions is intentionally OUT (partner-gated).

### F-63 [improvement] (lenses: research:legal; effort S)
**12-minute PT timeline has no slack: §6.1 packs 7 min of D.1 + 4 min of supplementary demos + opening into 0:00-12:00 with zero buffer for 3 live hero moments and zero transition time; live multi-file F4 export in 60s is physically tight**
- Location: 14_demo_pt.md:377-396 (§6.1 minute table), 13-23 + 14:495-496 (per-artifact PDF<=3s/BRF<=2s budgets)
- Issue: Research (CHANGED, arithmetic): §6.1 runs content blocks 0:00-12:00 then Q&A 12:00-15:00, but the table allocates D.1 golden flow (1:00-8:00 = 7 min) + D.5/D.6/tech-legal/발전방향 (8:00-12:00 = 4 min) + opening with zero buffer for the three live hero moments (each realistically 30-60s of unpredictable interaction) and zero transition time. Live hero #2 (F4 6-channel export, 5:00-6:00) gets exactly 60s for a live multi-file generation the plan elsewhere budgets at PDF<=3s/BRF<=2s per file across 4+ outputs — physically tight to impossible live. No slack line.
- Fix: Re-budget §6.1 with explicit transition/slack and verify live hero-moment durations against the per-artifact budgets (PDF<=3s × N). Either pre-generate during narration or lean on the existing T3 mention-only tiering for D.5/D.6. Demo-discipline hardening within existing T1/T2/T3 tiers, no scope addition.

### F-64 [improvement] (lenses: consistency, devex; effort S)
**P4 (단체 인솔자) has a tier rule but no matrix column and no golden case (un-testable, and the DB exposes group_leader the engine can't score); KTO key env-name and KMA weather host disagree across docs (ETL 500s / resilience test validates nothing)**
- Location: 16_suitability_policy.md:127,129-155 (P4 rule, no column) + 13:62-93 (no P4 golden) + 03:64 (group_leader) ; 04:254 (KTO_SERVICE_KEY_DECODING) vs 02:578 + 13:1432 (KTO_SERVICE_KEY) ; 04 §7 apihub.kma.go.kr vs 13:994 (data.go.kr/1360000)
- Issue: Two bundled correctness nits. (a) doc 16 §4.1 declares P4 and a P4 inheritance rule but the tier matrix has only 5 columns (no P4), and no golden case includes P4 — the rule is neither defined nor verifiable, and the DB enum exposes group_leader the engine can't score. (b) KTO key env name drifts: doc 04 transport reads KTO_SERVICE_KEY_DECODING but doc 02 §9 table and the kto-etl.yml workflow set KTO_SERVICE_KEY — the transport throws 'missing env' on every ETL run. (c) Weather host drifts: doc 04 §7 uses apihub.kma.go.kr but the doc 13 resilience test blocks data.go.kr/1360000 — different KMA portals, so the resilience test validates nothing about the real degradation path.
- Fix: (a) Either make P4 a pure derivation rule with a worked golden ('p4-group-strictest-member') or drop P4 + group_leader from MVP selection per §13.2 scope discipline; state the choice in §16 §4.1. (b) Pick one KTO key env name (KTO_SERVICE_KEY, already in the workflow) and update the transport + §9 table (note it stores the DECODING key). (c) Reconcile the KMA host (apihub.kma.go.kr vs data.go.kr/1360000), fix doc 04 §7 and the doc 13 resilience-test pattern to the same host via a shared constants module.

### F-65 [improvement] (lenses: design, devex; effort M)
**LIST=truth is stated but only the map gets component detail; the 'list' is called a 'fallback' and left implicit — plus two undefined-but-referenced surfaces (C1 'match' RPC residue, start:test script) stop owners at setup**
- Location: 05_feature_F1_total_guide.md:23-24,286,408,631 + 10:427 (PoiListFallback) ; 12_workstreams:90 (match RPC undefined) + 13:798 (start:test) vs 1839 (only start:demo)
- Issue: Two bundled items. (a) The strongest IA decision (LIST = source of truth, map = assist) is repeated as principle and the list components are named, but the map gets the richer treatment (CustomOverlay, centroids, keyboard) while the list's own visual/interaction design is implicit and it's even called 'PoiListFallback' (contradicting 'list=truth'). If the list is primary it deserves first-class design; otherwise sighted judges perceive the map as the 'real' UI. (b) C1 deliverables list a 'match' read-model RPC never defined anywhere (reads like pgvector match_documents residue, which SPEC cuts), and playwright.config launches pnpm run start:test but only start:demo exists — a fresh E2E run fails with missing script.
- Fix: (a) Give the primary LIST a first-class design pass in doc 05 §7: rename PoiListFallback to reflect primacy, specify its visual hierarchy and how slope/단차/회전 render as scannable text, and add persona-driven sort/filter (the user's critical capabilities float to top). (b) Define the read-model RPC name+signature in doc 03 or remove the stray 'match' token; rename the playwright webServer command to start:demo (or add a start:test script).

### F-66 [improvement] (lenses: devex; effort S)
**Suitability golden fixtures have two declared homes (__golden__ in doc 16 vs test-fixtures/suitability in doc 13) — if the engine and the worked-example generator read different dirs, the 'numbers are generated, not hand-maintained' guarantee silently breaks**
- Location: 16_suitability_policy.md:220 (packages/domain/policy/__golden__/) vs 13_testing_quality.md:58,136 (packages/test-fixtures/suitability/)
- Issue: doc 16 (policy authority) requires the 30+ golden cases in packages/domain/policy/__golden__/ and makes them the source that generates the 05 §4.5 worked example (the M-3 no-hand-numbers guard). But doc 13 points the golden test at packages/test-fixtures/suitability/ with a different file shape. If the engine reads goldens from one place and the worked-example generator from another, they drift and the numbers-are-generated guarantee silently breaks — the reproducible-formula credibility signal a judge probes.
- Fix: Make packages/domain/policy/__golden__/ the single home and update the doc-13 GOLDEN_DIR + file naming to match (or pick the other and state it as authority in 16 §11, with doc 13 linking). Add a CI assertion that the 05 §4.5 worked example is regenerated from that directory.

### F-67 [improvement] (lenses: ceo; effort M)
**Demo seed must deliberately tell a coherent two-POI story (공산성 polished hero vs 부소산성 improvable) — without an E2E assertion tying 부소산성 F1 정보없음 count to its F5 null_fields, the seed can drift into a live F1<->F5 contradiction**
- Location: 09_feature_F5_rto_dashboard.md:312-321 (부소산성 suitabilityAffected:true, priority=1.00) + 14_demo_pt.md:311-321 (seed checklist) vs 16_suitability_policy.md:205-206 (forced rules)
- Issue: The F5 demo seed sets 부소산성 elevator priority=1.00 (suitabilityAffected=true), which per the forced rules requires a selected-persona critical capability = unsupported OR unknown. If the seed makes 부소산성 elevator=unknown, then 부소산성's F1.A card MUST show 정보없음/대체추천 for a wheelchair persona — yet 부소산성 is a DEMO-PRIORITY full-evidence-pack POI expected to look complete. AC-F5-06 requires F5 null counts to equal F1 정보없음 counts, so this must be reconciled in the seed, not discovered live. A judge opening 부소산성 after seeing it as the F5 top gap finds either a consistent 정보없음 (undercutting the polished hero) or an inconsistency.
- Fix: Make the seed tell a deliberate two-POI story: 공산성 = polished high-coverage hero (F5 low priority, elevator gap = 본질제약/feasibility 0.1); 부소산성 = improvement candidate (F1 surfaces elevator 정보없음 with (b)운영자 미입력, F5 top priority=1.00 운영자 액션). Add an E2E/golden assertion tying 부소산성 F1 정보없음 count to its F5 null_fields (AC-F5-06) so the seed can't drift. Turns a potential live contradiction into a defensible 'good POI vs improvable POI' contrast. (Ties F-04 + F-12.)

### F-68 [improvement] (lenses: research:pdf-braille; effort S)
**react-pdf font-loading race + pdf-lib CJK subset hazards are unaddressed: Font.register is async but renderToBuffer can fire before fonts ready (intermittent tofu); pdf-lib subset flag is undefined (subset:true corrupts CJK, default false bloats every PDF)**
- Location: 08_feature_F4_diary.md:296,397-401,582,1177 (embedFont no subset flag; initKoreanFonts then immediate renderToBuffer)
- Issue: Research (CHANGED): (1) react-pdf Font.register initiates async load but renderToBuffer can run before fonts are ready with no built-in await -> intermittent tofu/missing-glyph PDFs (a classic intermittent demo failure); the plan calls initKoreanFonts() then immediately renderToBuffer with no readiness guarantee. (2) pdf-lib @pdf-lib/fontkit with subset:true drops/corrupts CJK glyphs and crashes on a dash; the plan's embedFont(bytes) leaves subset unspecified (default false is safe but embeds the full ~1-2MB Pretendard, bloating every PDF toward the 30s maxDuration/size limits).
- Fix: For react-pdf: read TTF bytes from disk and register via Buffer (not a URL/path that triggers async fetch), or await a font-ready hook, before renderToBuffer; add a non-flaky test. For pdf-lib: explicitly set embedFont(bytes,{subset:false}) and note the size cost in §11.2 budgets, or switch to pdf-fontkit (community fork fixing CJK subsetting). Document the chosen subset flag — currently undefined behavior. (Combines with F-19/F-33.)


## Per-lens verdicts

### ceo (6 findings)
The plan is genuinely strong on direction (the F1→F5 single-dataset lineage is a real differentiator, the locked decisions are coherent, and the depth-over-scope calibration is the right call for a generous-timeline contest). Prior rounds closed the formula, evidence-pack, validation, and runtime-defect blockers. But several NEW, high-leverage cracks remain in the parts a judge actually attacks: (1) the headline 데이터활용 claim "10 KTO services converge into one accessibility_facts table" is factually only ONE service writing that table — a sharp judge disbelieves the convergence diagram on inspection; (2) F5 presents 시군-level "공주 미입력 12건" numbers that are actually computed over only 3 curated POIs, exposing a gap between the proposal's "관광지 자동 식별, 시군별" promise and the 6-POI reality — the weakest spot in the lineage story; (3) the CACF 특별상 / +2 narrative has an owner-and-letter dependency (OI-9) with NO named human owner and a single fallback, which is the single largest swing on 발전성+지역특화 yet is run as a side-task, not a P0 with an accountable person; (4) there is no defined "single auditable lineage line" demo artifact even though prior reviews named it the highest-leverage move; (5) the diary→F4→next-visitor reuse loop is asserted but the GPX a diary emits is the curated F1.B GPX, not a user-generated one, so "다음 방문자가 그대로 따라갈 검증된 코스로 환류" is weaker than the slide implies. None of these require restoring cut scope; all are depth/framing/validation hardening that raise the score. Fix the two over-claims before they reach slides, name a human owner for CACF, and build one bulletproof lineage exhibit.

### eng (10 findings)
Reading the four target docs plus SPEC §6/§7 and doc 16, I found a tightly-clustered set of NEW cross-document correctness landmines the prior M-list did not catch — all of the "compiles fine, dies silently at demo" class an EM hunts for. The most dangerous: (1) FOUR mutually incompatible capability_code naming schemes across the four authoritative-by-domain docs (03 uses `entry.wheelchair`, 04 uses `MOBILITY_WHEELCHAIR_ENTRY`, 16 uses `wheelchair_access`, and the doc-13 contract tests assert `BF_BRAILLE_BLOCK`/`BF_ENTRY` which match NONE of them) — the join key between ETL→DB→domain is undefined, and the golden/contract tests as written cannot pass; (2) the atomic-publish "last-good-served" guarantee — the spine of the entire ETL-failure-isolation story a judge will probe — is unimplementable as written: doc 02 §6.2 read-model filters `WHERE published_version=(...active=true)` but the canonical tables have NO version column and doc 03's publish_dataset() physically delete-then-inserts, and the two docs disagree on dataset_versions' shape (single `dataset='main'`+`active` column vs per-dataset PK+`row_status`); (3) THREE different HMAC revalidate conventions (header `x-revalidate-signature`+env `REVALIDATE_HMAC_SECRET` vs header `x-etl-signature`+env `ETL_HMAC_SECRET` vs the canonical workflow's `Authorization: Bearer`+`REVALIDATE_HMAC_TOKEN`) → guaranteed silent 401/403 on every publish, cache never invalidates, demo serves stale data; (4) Odii xCoord/yCoord datum is never specified yet stored as SRID 4326 and geofence-compared to WGS84 GPS — if Odii returns Korean projected coords the docent geofence silently never fires. Plus a revalidate-tag namespace mismatch, KTO key env-name drift, KMA endpoint contradiction, and a weather data-plane/runtime contradiction in the resilience test. None of these touch a SPEC §2 locked decision or §7/§16 formula value — they are integration-contract drift fixable by picking one authority per concern.

### devex (8 findings)
Test summary to validate schema parsing.

### design (10 findings)
As a senior product designer auditing a barrier-free heritage app where design IS the accessibility argument, the plan's information-architecture spine (LIST=truth, the 4-Layer evidence card, KWCAG primitives, contrast tokens, focus/landmark patterns) is genuinely strong — this is a 7-8/10 component-and-compliance foundation. But the plan is almost entirely a structural/data spec with a critical hole in the EXPERIENTIAL layer that a 공모전 judge experiences first and remembers longest. The single biggest gap: the multi-persona SELECTION input — the literal first interaction, the "휠체어 할아버지 + 손녀 in one flow" promise, and the thing the rubric explicitly rewards (기획력 30) — has NO designed UI anywhere; it exists only as a bare component name `<PersonaSelector>` and an STT voice-over in the PT script that the live demo explicitly does NOT use. Second: EMPTY/UNKNOWN states (정보없음, zero-result, first-run, empty diary) — the emotional make-or-break of a barrier-free product where "정보 없음" is the COMMON case, not the edge — are undesigned across all five features; the plan repeats the data rule ("정보 없음 — 현장 확인 필요") but never gives it warmth or a next action, so it currently reads as a dead end that makes the dataset look thin (the exact opposite of the F1→F5 lineage thesis). Third: the hero 4-Layer evidence card is specified as an ASCII table of formula internals (A×B×C×D, weights, multipliers) — defensible to an engineer, but a judge cannot read "왜 이 점수인지" from a multiplication table in 5 seconds; the card has no designed hierarchy that answers the human question first ("can MY group go, and what's the one thing to watch") before the math. Dimension ratings (what a 10 looks like noted per finding): Information hierarchy 8/10; Evidence-card-as-hero 5/10; Empty/unknown states 2/10; Multi-persona input UX 2/10; Cognitive-friendly (예측가능) mode 6/10; F4 6-output moment 7/10; AI-slop avoidance 6/10; Trust-at-pixel 7/10; First-5-sec/5-min/5-year arc 3/10. The fixes are depth/validation investments (design specs, content tone, motion/empty-state systems), not new scope — fully aligned with the locked "invest runway in depth" calibration.

### office-hours (6 findings)
Through the YC office-hours lens this is a beautifully engineered answer to a question nobody in the plan has yet been observed to ask. Six forcing questions: (1) Demand reality — the "5 questions 관광약자 face" are the load-bearing premise of the whole entry, yet they are asserted from the proposal and policy environment with ZERO primary observation anywhere in the 25-file research corpus (no interviews, survey, n=, or baseline pain log; grep confirms none). (2) Status quo — the side-by-side "Wheelmap pin vs 4-Layer card" (14_demo_pt.md §2.2) shows the product is more information-rich, but the plan never demonstrates a 관광약자 reaches a DIFFERENT, BETTER decision than calling ahead / 모두의여행 today; richer ≠ 10x. (3) Desperate specificity — the one user is the wheelchair할아버지+손녀 D.1 persona, but he is a demo prop, not a recruited real person with a real upcoming trip. (4) Wedge — 공산성 F1→F5 is the right shape, but 공산성 is a HILLTOP FORTRESS and the plan bakes `wheelchair_access=supported` into the worked example (05 §4.5) and demo seed (14 §5.2) BEFORE the July on-site verification that could falsify it, with no fallback POI. (5) Observation-over-assertion — the single most load-bearing unobserved claim is exactly "공산성 is wheelchair-traversable end-to-end." (6) Future-fit — the F3→데이터품질 flywheel and CACF B2G handoff are both asserted but un-piloted. SINGLE RIServICEST ASSUMPTION: that the chosen hero POI (공산성) is genuinely wheelchair-accessible enough to carry a flawless wheelchair-할아버지 live demo. It is currently UNVERIFIED and load-bearing for the entire D.1 narrative; verification is scheduled for July, after the demo script and seed already commit to the answer.

### consistency (11 findings)
Reading SPEC.md, SPEC.ko.md, 16_suitability_policy.md, 03_data_model.md, 05_feature_F1_total_guide.md, 13_testing_quality.md, 06_feature_F2_docent.md and 04_kto_data_integration.md, I found a cluster of NEW contract/consistency defects the prior rounds (RC-1..RC-4, B/M series) did not catch. The most damaging: there is NO single, consistent capability_code vocabulary — four mutually incompatible naming systems coexist across the four documents that all claim to drive the same calculateSuitability engine, so the persona×capability tier matrix (doc 16 §4.1), the DB catalog (doc 03), the F1 mapping table (doc 05) and the contract tests (doc 13) literally cannot bind to each other. A judge tracing the one-dataset lineage from accessibility_facts → Layer A/B → score card → golden tests will hit a different code string at every hop. Compounding this, doc 16 internally self-contradicts on the coverage<0.65 label (정보없음 in §9 vs 주의-cap in §6), the dual SPEC files both declare themselves the single authority with no deference clause, two embedded CREATE TYPE blocks disagree on enum name+values (absence + docent channel), and buildItinerary/PersonaId/import-alias signatures diverge between docs — several of these are SQL/TS that won't compile or tests that cannot pin an expected value. None touch a SPEC §2 locked decision or the §7/§16 formula numbers; all are naming/wording/contract reconciliations.
