# 09 — 화면을 직접 열어 보고 고친 것

> **한 줄:** 브라우저로 전 화면을 돌아 보고, 도메인·라우트·문안을 적대적으로 검토했다. **가장 무거운 것은 `resolveStatus()`가 확인된 부재를 `supported`로 발행하고 있었던 것**이다 — 「장애인용 화장실이 설치되어 있지 않습니다」가 **「확인됨」**으로 찍혔다.
>
> 08번이 「데이터가 붙었다」였다면 이 회차는 **「붙은 데이터가 화면에서 무엇으로 보이는가」** 다.

---

## 1. 안전 판정이 뒤집혀 있었다 — `resolveStatus()`

### 1.1 무엇이 일어났나

부정 사전 `NEGATION`은 **어미까지 붙은 특정 연어**를 나열했다(`설치되지\s*않`, `운영하지\s*않`, `제공하지\s*않`). 긍정 사전 `PRESENCE`는 **어간만** 봤다(`설치되어`, `운영`, `제공`, `대여`, `비치`, `가능`).

한국어의 부정은 **접미**다. 그래서 **부정형 문장 안에는 언제나 그 동사 어간이 그대로 들어 있다.** 나열에 없는 부정 표현은 `NEGATION`을 그냥 지나쳐 `PRESENCE`에 걸렸다.

실제 측정 결과 — 전부 **`supported`(확인됨)** 로 판정됐다:

| 원문 | 뜻 | 고치기 전 |
|---|---|---|
| `장애인용 화장실이 설치되어 있지 않습니다` | 장애인 화장실이 **없다** | **supported** |
| `계단으로만 이동 가능` | **계단뿐이다** | **supported** |
| `엘리베이터 고장으로 운영 중단` | 엘리베이터가 **멈췄다** | **supported** |
| `운영 안 함` · `제공 안 함` | 운영·제공 **안 한다** | **supported** |
| `대여하지 않음` · `비치하지 않음` · `휠체어 대여 안 됨` | **빌려주지 않는다** | **supported** |

`restroom`은 P1a·P1b·P3의 필수 항목이다. `supported`가 되면 `knownCriticalBlockers`에도 `unknownCriticals`에도 들어가지 않고, 규칙 1이 발동하지 않는다. **장애인 화장실이 없는 곳이 휠체어 이용자에게 「방문가능」으로 보일 수 있었다.**

스펙(`05_ingest.md` §4.2)이 경고한 두 방향은 ①「장벽 없음」을 부재로 읽는 것과 ②「확인된 부재」를 `unknown`으로 읽는 것이었다. **이건 셋째 방향이고 둘보다 무겁다** — 확인된 장벽을 **확인된 시설**로 발행한다.

같은 결함의 약한 형태도 있었다: `엘리베이터 없어요`·`수유실 없어요`·`엘리베이터 미비`·`경사로 파손` → 전부 `unknown`.

### 1.2 어떻게 고쳤나 — 목록을 늘리지 않았다

**부정을 「구절」이 아니라 「형태」로 본다.** 장형 부정(`-지 않-`, `-지 못-`), 단형 부정(`안 V`, `못 V`), 존재 부정(`없-`), 한자 접두 부정(`미-`, `불-`), 그리고 시설이 멈췄다는 말(`중단`·`중지`·`폐쇄`·`고장`·`파손`).

**그리고 긍정도 국소적으로 읽는다.** `scanBarriers()`가 장애물 명사 옆을 보는 것과 같은 방식으로, 시설 어간 **뒤** 창에 부정 표지가 있으면 그 어간은 존재의 증거가 아니다. 어간이 부정형 안에 들어 있다는 사실 자체가 원인이었으므로, 원인을 없애는 자리가 여기다.

**셋째로, 극성을 읽지 못한 장애물 명사는 이제 긍정 판정을 막는다.** `계단으로만 이동 가능`은 `계단` 주변 8자 안에 부정·존재 표지가 없어 장애물이 **조용히 버려지고** `가능`이 문장을 혼자 결정했다. `BarrierScan.ambiguous`가 이 경로를 막고, 덤으로 `PRESENT_NEARBY`에 배타 조사 `만`을 넣어 `계단으로만`이 제대로 `unsupported`가 된다.

**회귀 테스트 42건은 한 건도 바뀌지 않았고, 위 12개 문장이 테스트로 들어갔다**(`resolve-status.test.ts`, 54건).

---

## 2. 화면이 KTO 카탈로그 제목을 그대로 쓰고 있었다

`scripts/ingest.ts`가 `detailCommon2.title`을 표시 이름으로 저장했다. 그 값은 **카탈로그 표제**이지 우리가 다루는 자산의 이름이 아니다.

| slug | 고치기 전 (화면) | 우리 이름 |
|---|---|---|
| gongsanseong | `공주 공산성 [유네스코 세계유산]` | `공산성` |
| muryeong-royal-tombs | `공주 무령왕릉과 왕릉원[유네스코 세계유산]` (앞 공백 없음) | `무령왕릉과 왕릉원` |
| **busosanseong** | **`관북리유적과 부소산성 [유네스코 세계유산]`** | **`부소산성`** |
| jeongnimsaji | `부여 정림사지 오층석탑 [유네스코 세계유산]` | `정림사지` |

세 가지가 한꺼번에 잘못됐다. **①** 접미 `[유네스코 세계유산]`이 바로 아래 줄의 「유네스코 백제역사유적지구 구성유산」과 겹쳤고 띄어쓰기가 제각각이었다. **②** 영문 제목은 `Busosanseong Fortress`인데 국문은 `관북리유적과 부소산성`이라, 한 레코드의 두 로케일이 **서로 다른 장소**를 가리켰다. **③ 가장 무거운 것** — 이 서비스의 사실·경로·점수는 전부 **부소산성만** 다룬다. 화면이 관북리 유적까지 다루는 것처럼 말하는 것은 **사실과 다르다**. `11_open_items.md` P0-7이 「구성유산의 일부라고 쓴다」로 정해 둔 것과도 어긋난다.

`content/pois.json`의 `nameKo`/`nameEn`이 국가유산청 지정명칭으로 이미 확인돼 있고 스키마가 비어 있는 것을 허용하지 않는다. **표시 이름은 우리 것이 이기고, 개요·주소·전화·홈페이지는 그대로 KTO에서 온다.**

---

## 3. 사진 130장 중 상당수가 다른 곳 사진이었다

`gallerySearchList1`은 키워드를 느슨하게 맞춘다. `공산성`으로 조회한 결과에 **`백제군사박물관`(논산)·`공주 금강철교`·`미르섬`** 이 들어 있었고, 공산성 한 화면에 **이미지 130장**이 붙었다.

두 가지가 한 문제였다. **다른 곳 사진을 이 관광지 이름 아래 두는 것은 틀린 말이고**, 130장은 전부 `/api/image-proxy`를 지난다 — KTO 자산은 https 직접 제공이 0건이라 프록시가 유일한 경로다(08번 §7).

**고친 방식:** 제목이 이 관광지를 실제로 지칭하는 행을 앞으로 보내고 **8장에서 끊는다**(`GALLERY_PHOTO_LIMIT`). 버리지 않고 순서를 바꾼 이유는 **낙화암·군창지·백화정·반월루가 부소산성 안에 있으면서 제목에 그 이름을 쓰지 않기 때문**이다 — 제목만으로 거르면 맞는 사진이 함께 날아간다. 그리고 **각 사진의 제목을 화면에 함께 찍는다**. 사진 옆에 이름이 없으면 그 사진은 이 장소의 사진이라고 주장하는 것이 된다.

결과: 관광지당 **8~18장**(이전 47~130장).

---

## 4. 스크린리더에서 두 단어가 한 단어로 읽히던 자리 6곳

인접한 인라인 요소 사이에 **공백 문자가 없으면** 접근성 트리에서 한 낱말이 된다. CSS `margin`은 텍스트가 아니다.

| 화면 | 읽히던 소리 |
|---|---|
| 홈 조건 체크박스 | `휠체어를 이용해요P1a` |
| 관광지 상세 축 제목 | `진입entry` |
| 갭 리포트 항목 열 | `휠체어wheelchair` |
| 제보 폼 동의 | `[필수]위 내용이…` |
| 제보 카드 | `경사로 막힘2026-09-12 목격` |
| 갭 리포트 방문자 | `79,241.3명외지인(b) 기준` |

기계 코드(`P1a`·`entry`·`wheelchair`)는 **스펙과 데이터셋을 나란히 놓고 보는 사람을 위한 상호참조**이므로 `aria-hidden`으로 접근성 트리에서 뺐다. 날짜·표지처럼 **읽혀야 하는 것**에는 공백을 텍스트로 넣었다.

---

## 5. 문서 구조와 라우팅

### 5.1 제목 단계가 평평했다

`판정에 들어간 항목`(h2) 아래 여섯 축이 **같은 h2**였고 항목이 h3였다. 축이 그 절의 자식이라는 것이 제목 목록에서 드러나지 않았다. 축 h3 · 항목 h4로 내렸다.

도슨트 화면은 더 나빴다 — 이야기 12편이 각각 h2 제목을 갖고, 그 **대본도 h2**였다. 스크린리더의 제목 목록에 **`대본`이 12번 나란히** 뜨고 어느 이야기의 것인지 알 방법이 없었다. 대본을 h3로 내렸다.

### 5.2 주소가 틀렸을 때 빈 문서가 나왔다

`/ko/places/없는곳`은 **`<html id="__next_error__">`** 에 본문이 하나도 없는 문서를 냈다. `lang`도 제목도 본문도 없다 — 눈으로 보면 백지이고 스크린리더에는 침묵이다. **이 서비스에서 가장 나쁜 화면이고, 아무도 보지 않는 화면이다.**

원인은 구조다. 이 앱은 라우트 그룹 셋이 각자 루트 레이아웃을 갖고 있어(`(site)`·`(admin)`·`(print)`) **`app/layout.tsx`가 없고**, Next이 not-found 렌더를 감쌀 레이아웃이 없다. `[locale]` 아래에 `not-found.tsx`를 둬도 내용만 나오고 문서 껍데기는 여전히 Next의 것이었다.

**그래서 not-found 특수 파일에 기대지 않는다.** 없는 slug는 페이지가 직접 안내 화면을 그리고, 로케일 아래 어떤 주소도 받지 않는 경로는 `[...rest]` 캐치올이 받는다. 둘 다 **진짜 레이아웃 안에서** 렌더된다 — 언어·건너뛰기 링크·주요 메뉴·긴급 연락 버튼이 전부 제자리에 있다.

**대가는 상태 코드다.** 이 화면들은 404가 맞는 자리에서 200을 답한다. 로케일 트리 **밖**의 주소는 `src/app/not-found.tsx`가 404로 답한다. 백지 문서와 200 사이에서 후자를 골랐고, 이건 되돌릴 수 있는 선택이다 — `app/layout.tsx` 하나로 세 그룹을 다시 묶으면 둘 다 얻는다.

**axe 검사 경로에 `/ko/nope`·`/en/nope`·`/ko/places/no-such-place`를 넣었다.** 그리고 **이 검사가 실제로 빨간불이 되는지 확인했다** — 캐치올의 `<h1>`을 `<p>`로 바꾸고 링크의 이름을 지우자 즉시 실패했고, 되돌리자 통과했다.

### 5.3 언어 전환이 홈으로 던졌다

`<Link href="/" locale={other}>`. 관광지 상세를 읽다가 English를 누르면 **영어 홈**으로 갔다. `LocaleSwitch`가 `usePathname()`으로 현재 경로를 유지한다.

### 5.4 브라우저 탭에 slug가 찍혔다

`gongsanseong · 오디오 해설` · `jeongnimsaji · 경로 안내`. 두 화면의 `generateMetadata`가 관광지 이름 대신 데이터베이스 키를 썼다.

---

## 6. 서버 경로와 데이터

### 6.1 조회 실패가 「제보 없음」으로 보였다

`ReportsSection`이 502 응답에서 `body.reports ?? []`를 읽어 **빈 목록**으로 렌더했다. Supabase가 흔들리는 동안 모든 관광지 화면이 **「아직 올라온 제보가 없습니다」를 사실로 단언**했다. 이 서비스가 다른 모든 자리에서 거부하는 바로 그 추론이다. 상태를 `loading` / `ready` / `unavailable` / `failed` 넷으로 나눴다.

### 6.2 `/api/report`가 아무 slug나 받았다

`poiSlug: z.string().min(1)`. 하루 한 번 제한은 `unique (reporter_id, poi_slug, category, created_day)` 하나뿐인데 **slug가 그 키의 절반**이라, 값을 바꿔 가며 보내면 제한이 통째로 사라졌다. 게다가 그렇게 들어온 행은 **어느 화면에도 보이지 않으면서** 관리자 목록만 채운다. `content/pois.json`의 slug 집합으로 검증한다.

### 6.3 갭 리포트 CSV

- **수식 주입.** `=`·`+`·`-`·`@`로 시작하는 셀을 Excel이 수식으로 실행한다. 인용부호는 막지 못한다 — Excel은 `"=1+1"`도 계산한다. 관광지 이름 열은 **KTO 응답 문자열**이고 이 파일은 담당 공무원이 Excel로 열라고 BOM까지 붙여 만든다. 선행 어퍼스트로피로 중화한다. 홑 `\r`이 레코드를 쪼개던 것도 같이 고쳤다.
- **`?locale=en`이 절반만 동작했다.** 제목은 영문인데 상태·원인 열과 머리글은 한국어였다.
- `revalidate = 3600`이 **아무 일도 하지 않고 있었다**(핸들러가 `request.url`을 읽으므로 매번 동적 렌더). `force-dynamic`으로 사실을 적었다.

### 6.4 관리자 화면

- **멤버십 조회 오류를 「관리자가 아닙니다」로 표시했다.** 일시적 오류에 진짜 운영자가 없는 권한 문제를 찾으러 간다.
- **Postgres 오류 원문을 화면과 액션 반환값에 실었다.** 테이블·컬럼·정책 이름이 들어 있고, 이 화면은 세션만 있으면 누구나 연다. 로그로 옮겼다.
- **페이지 넘기기가 없었다.** 200행 상한에 신고순 정렬이라, 신고가 200건을 넘으면 **신고되지 않은 첫 제보의 자리가 사라진다**. 신고는 익명 세션 하나면 되므로 「사람만 신고한다」는 전제는 없다. 50행 페이징 + **「신고 안 된 것」 보기**를 넣었다. 04번 §4가 「제보가 쌓이면 그때」로 미뤄 둔 항목이다.

> **닫지 못한 것 하나.** `flag_report`에는 여전히 호출자·건수·시간 제한이 없다. `002_reports.sql`의 주석은 `authenticated` 전용으로 좁혀 막았다고 적었는데, **Supabase에서 익명 세션이 곧 `authenticated`** 이므로 막히지 않았다. 페이징이 피해를 없애지만 원인은 남아 있다. 진짜 해결은 `(report, flagger)` 유일 인덱스를 갖는 `003_report_flags.sql`이고, 그건 스키마 변경이라 **사람이 결정할 일**로 남긴다.

### 6.5 오류 화면이 없었다

`src/app` 어디에도 `error.tsx`가 없어서, 알려지지 않은 예외는 Next의 영문 한 줄로 끝났다. `(site)/[locale]/error.tsx`를 넣었다 — 오류 원문은 찍지 않고 `digest`만 남긴다.

---

## 7. 도메인 결함 넷

| 무엇 | 증상 |
|---|---|
| `pickAlternatives` | 자기 자신이 `대체추천`일 때 **같은 `대체추천` 관광지를 대안으로 추천**했다. 확인된 장벽이 있는 곳이 「대신 여기」가 된다. 스펙 06 §6.5가 이 고리를 막으려고 점수가 아니라 라벨로 비교하게 한 것인데 코드가 되살렸다 |
| 규칙 2 둘째 항 | 필수 항목이 **전부 `not_applicable`** 인 페르소나가 커버리지 0인 채로 **점수를 받았다**. 가드가 `requiredFacts.length`가 아니라 `personaIds.length`에 걸려 있었다 |
| `unknownCriticals` 순서 | 페르소나를 **고른 순서**를 따랐다. 화면의 「확인 필요:」 문장이 어느 칩을 먼저 눌렀는지에 따라 달라졌다. 카탈로그 순서로 고정 |
| `localeCompare` 결정성 | 로케일 인자 없는 정렬은 런타임 ICU에 의존한다. 서버와 브라우저가 같은 스냅샷을 다르게 정렬할 수 있고, 그게 tie-break가 존재하는 이유다. 코드포인트 비교로 교체 |

그리고 **UTC 미끄러짐**: `useToday()`·`useDiary()`·`ReportRow`가 `new Date().toISOString().slice(0,10)`을 썼다. `scripts/ingest.ts`의 `seoulToday()` 주석이 **바로 그 표현을 쓰지 말라고 명시**하고 있었는데 브라우저 쪽 셋이 그대로 썼다. KST 00:00~09:00 사이에 **어제 날짜**가 되어 신선도 구간을 한 칸 밀고, 여행 기록의 날짜와 파일명을 하루 앞당긴다. `src/domain/today.ts` 하나로 합쳤다.

GPX는 XML 1.0이 금지하는 C0 제어문자를 그대로 내보냈다 — 어떤 이스케이프로도 합법이 되지 않는 문자라 제거한다.

갭 리포트 우선순위 표가 **파생 항목 9개까지 올리고 있었다**. 같은 페이지의 머리글과 채움률 표는 「한국관광공사 항목 기준」이라고 말한다. 담당 공무원에게 「당일 기상 예보 — 정보 없음」을 채우라고 하는 표였다. KTO 항목만 남겼다.

---

## 8. 문안

### 8.1 스펙이 금지한 표현을 화면이 쓰고 있었다

`07_screens.md` 전 화면 공통의 **금지 표현** 한 줄: *「공사」를 한국관광공사의 줄임말로 쓰지 않는다 — 이 앱에는 제보 분류 `construction`(「공사 중」)이 있어서 같은 두 글자가 정반대 뜻으로 읽힌다.*

갭 리포트의 채움률 표 머리글이 **`공사 원문`** 이었다. 무장애 시설 표에서 「공사」는 정확히 반대로 읽힌다. `한국관광공사 확인`으로 바꿨다(옆 칸 `우리가 확인`과 짝이 맞는다).

### 8.2 용어

| 고치기 전 | 고친 뒤 | 왜 |
|---|---|---|
| `수화 안내` | **`수어 안내`** | 「한국수화언어법」(2016)이 한국수어/수어를 법정 용어로 정했다. 농인이 쓰는 말도 수어다 |
| `화장실` | **`장애인 화장실`** | 영문은 `Accessible restroom`인데 국문만 「화장실」이라, 두 로케일이 **다른 것을 주장**했다. 휠체어 이용자가 가장 먼저 보는 항목이다 |
| `주차` / `관람석` / `객실` / `객실(청각)` | `장애인 주차구역` / `장애인 관람석` / `휠체어 이용 가능 객실` / `청각장애인 편의 객실` | 같은 비대칭 |
| `휠체어` / `유모차` | `휠체어 대여` / **`유아차 대여`** | KTO 필드는 대여 여부다. 그리고 홈 화면은 이미 「유아차」를 쓰고 있었다 |
| `derived` | `파생 항목` | 한국어 화면에 영문 개발 용어 |

### 8.3 문장

- `places.noVerdict` — `"{persona} 이용에 꼭 필요한…"` 에 `persona`가 `휠체어 이용`이라 **「휠체어 이용 이용에 꼭 필요한」** 이 찍혔다. 조건 미선택이면 「조건 미선택 — 일반 방문 기준 이용에」
- `place.whyCautionUnknown` — `「{items}」을 모릅니다` 의 조사가 고정이라 **「엘리베이터」을** 이 나왔다
- `home.minRuleTitle` — 「가장 조건이 **까다로운** 분」. 동행에게 쓰면 까다롭다는 「성격이 별나다」는 뜻이 된다. 무장애 서비스가 휠체어 이용자를 그렇게 부르고 있었다. → 「도움이 가장 많이 필요한 분」
- 피동·명사형 정리 12곳, 「이번 범위」 같은 **스펙 내부 어휘** 제거

### 8.4 영어

`status`/`absence`/`label` 문구, `외지인(b)` 같은 API 원문 노출, 한국어만 있던 방문자 주의 문구, KOGL 유형의 영문 표기, 출처·이용조건 표 전체를 영문으로 채웠다. 그리고 **영어 화면에 남아야 하는 한국어에는 `lang="ko"`를 달았다** — 원문 인용, 출처 줄, 지정명칭, 사진 제목, 시설 이름, 연관 관광지 이름. 없으면 영어 음성이 한글을 영어 음소로 읽는다.

### 8.5 개발자 메모가 공개 화면에 찍히고 있었다

`content/safety-directory.json`·`content/facilities.json`의 `sourceNote`가 인용과 편집 메모를 한 필드에 담고 있었고, 그 메모가 해라체 그대로 `/credits`와 관광지 상세에 나왔다 — 「지원 언어 개수는 이 페이지에 없어 적지 않는다」, 「좌표는 공식 출처에서 확인하지 못해 비웠다」. 읽는 사람에게 쓸모 있는 것은 남기고 문체를 맞췄다.

`이 점수가 나온 계산`의 마지막 줄 `P0 · policy suitability-v5`와, 아무 이름 없이 찍히던 `0.60 × 0.90`도 같은 종류였다.

---

## 9. 숫자와 단위

- **`예측 혼잡도 82.94`** — 매뉴얼에 단위도 분모도 상한도 없는 값을 소수점까지 그대로 찍었다. 읽는 사람이 할 수 있는 일이 없다. → `혼잡 — 집중률 82.9 (40 이하 여유 · 70 이하 보통). 2026-09-13 기준, 향후 30일 예측치입니다`
- **`79,241.3명`** — 사람에 소수점. 반올림
- **`20260807 ~ 20260814`** — KTO 요청 파라미터 형식을 그대로. `2026-08-07 ~ 2026-08-14`
- **`체류 90분 → 90분`** — 조건이 시간을 바꾸지 않았을 때도 화살표를 그렸다. 같은 숫자 두 번은 렌더링 오류로 보인다

그리고 **`상황` 축의 상태 어휘**: 혼잡도 82에 「**이용 불가**」가 붙었다. 무장애 서비스에서 그 말은 「갈 수 없다」로 읽힌다. `기상 특보`·`응급실 거리`도 같은 어휘를 썼다. 상황 축만 `양호 / 주의 / 나쁨`을 쓴다. **해당 없음**도 배지를 얻었다 — 설명은 「해당하지 않는 항목입니다」인데 배지는 「정보 없음」이라 서로 다른 말을 하고 있었다.

---

## 10. 여행 기록이 `/en`에서도 한국어였다

04번 §3.4가 「비용이 크다고 판단해 넘겼다」로 남겨 둔 항목이다. `src/domain/diary.ts`가 라벨을 한국어로 박아 놓아서 `/en/diary/print`는 **영어 화면 안에 한국어 문서**를 출력했고, `.txt` 내보내기는 어느 로케일에서도 한국어였다.

`DiaryLabels`를 호출자가 넣어 주는 형태로 바꿨다(`src/lib/diary-labels.ts`). 지정명칭과 경로 근거 문장은 **인용이므로 한국어로 남는다** — 번역하면 대조할 수 없는 이름이 된다.

---

## 11. 그 밖

- **파비콘이 404였다.** 무령왕릉 전축분의 터널 볼트 — 6곳이 공유하는 유일한 실루엣이고 16픽셀에서 읽히는 유일한 백제 형태다. 서체 의존이 없도록 path로 그렸다
- **첫 화면 진입마다 `<h1>`에 포커스 링이 그려졌다.** `RouteFocus`가 `useRef` 불리언으로 첫 렌더를 걸렀는데, React는 개발 모드에서 effect를 두 번 돌린다 — 첫 번째가 깃발을 내리고 두 번째가 통과했다. 마운트 시점의 **경로**를 기억하면 몇 번을 돌든 결과가 같다
- `src/i18n/routing.ts`의 「일문·중문도 저장한다」 주석은 08번 §5에서 사실이 아니게 됐다

---

## 12. 배포하면서 드러난 것 — 수집이 「못 봤다」를 발행했다

야간 수집(GitHub Actions)에 저장소 시크릿 5개를 넣고 처음 돌린 날 일어난 일이다.

러너에서 `apis.data.go.kr`로 가는 **모든 호출이 `fetch failed`** 로 끝났다. 그런데 수집은 **그대로 완주해서 `pois` 스냅샷을 발행했고**, `media`가 6곳 전부 빈 배열이었다. 관광지 화면 여섯 개가 실제로 존재하는 사진 89장에 대고 「이 관광지 사진은 아직 모으지 못했습니다」라고 말하기 시작했다. `bootstrap` 단계는 그 전에 이미 코드표 2,338줄을 **빈 배열 두 개**로 덮었다.

**원인은 네트워크가 아니라 발행 규칙이다.** 단계가 payload를 조립하는 시점에 이르면 **「응답 없는 호출」과 「빈 응답」이 똑같은 빈 배열**이다. 모든 단계가 조립한 것을 그대로 발행하므로, 아무것도 보지 못한 실행이 그 못 봄을 세상에 대한 사실로 발행했다.

**고친 것:** `transport.ts`가 게이트웨이에 **닿은 호출과 닿지 못한 호출을 센다.** 모든 호출이 지나는 단 하나의 깔때기에서 세므로 호출부가 기억할 것이 없다. `resultCode`는 답이므로 접촉으로 친다 — 오직 전송 실패만 「닿지 못함」이다. 그 수가 1 이상이면 `ingest`는 **파일도 데이터베이스도 쓰지 않고 종료한다.** 오늘을 보지 못한 실행의 올바른 출력은 어제의 스냅샷이고, 이건 `abortOnQuota`가 할당량에 대해 이미 내리고 있던 판단과 같다.

**이 검사가 실제로 빨간불이 되는지 확인했다.** `GATEWAY_BASE`를 없는 호스트로 바꾸자 첫 단계에서 「2 call(s) never reached the gateway (0 did)」로 종료했고, `content/generated`는 바이트 단위로 그대로였다. 되돌리자 정상 발행했다.

**그 뒤:** 같은 워크플로를 다시 돌리자 **2분 만에 전 단계 성공**했다(`bootstrap`·`pois` 6·`routes` 2·`context` 12·`accessibility` 198·`docent` 222·`related` 3). 처음 실패는 일시적이었다 — 직전에 잘못된 키로 돈 실행이 만든 호출 폭주 때문으로 보인다. **cron은 그대로 둔다.** 가드가 있으니 못 닿는 밤은 빨간불 하나로 끝나고 데이터는 어제 것을 유지한다.

> **배운 것 한 줄:** 수집 파이프라인에서 「실패를 경고로 남기고 계속 간다」는 **부분 실패를 사실로 승격시키는 경로**다. 경고는 사람이 읽지만 스냅샷은 화면이 읽는다.

---

## 12b. 사진이 한 장도 뜬 적이 없었다

배포 후 실제 URL에서 픽셀을 세어 보고 나온 것이다. 그 전까지 나는 `<img>` **태그 수**를 셌고, 태그는 17개 있었다.

**독립된 결함 둘이 겹쳐 있었다.**

**① `tong.visitkorea.or.kr`은 모든 경로에서 HEAD에 405를 답한다.** `resolveImageUrl()`은 HEAD로 https 제공 여부를 확인하고 실패하면 `/api/image-proxy`로 보낸다. 그래서 **모든 이미지가 프록시로 갔다** — 08번 §7이 「460건 중 https 직접 제공 0건」으로 적고 Vercel 함수 시간과 대역폭 비용을 남은 질문으로 올려 둔 그 숫자다. **그 비용은 실재하지 않았다.** 같은 URL이 Range GET에는 206과 이미지 바이트를 준다. 현재 스냅샷의 89개 URL **전부** 확인했다.

**② 프록시는 받은 것을 하나도 내주지 않았다.** 허용 목록이 등록된 미디어 타입(`image/jpeg`…)을 담고 있는데 업스트림은 **`image/jpg`** 를 보낸다 — 등록되지 않은 표기다. `startsWith` 비교가 전부 빗나가 **모든 KTO 사진이 415**로 거절됐고, 그걸 받은 Next 이미지 최적화기가 다시 400을 냈다.

§1의 `resolveStatus`와 **같은 모양의 결함**이다 — 닫힌 목록이 열린 세계를 만난다.

**고친 것:** 탐침을 1바이트 Range GET으로 바꿨다(프록시 경유 **89건 → 0건**). 프록시는 `image/jpg`를 받아 **`image/jpeg`로 정규화해서** 내보낸다 — 바이트는 JPEG이고 헤더가 그렇게 말해야 한다. 라우트 계약을 `src/app/api/image-proxy/route.test.ts` 9건으로 고정했고, **틀렸던 그 케이스가 첫 번째 테스트**다.

**검증은 태그가 아니라 픽셀로 했다.** `/ko/places/gongsanseong`에서 17장 중 **17장이 디코드**되고 `naturalWidth`가 640이며 프록시 요청은 0건이다. 로컬 프로덕션 빌드와 배포된 `modu-baekje.vercel.app` 양쪽에서 확인했다.

> **배운 것 한 줄:** 「요소가 있다」와 「보인다」는 다른 주장이다. 빈 상태 문구가 정중하면 실패가 정상처럼 보인다.

---

## 12c. 사람이 정할 것 셋 — 전부 결정됐다 (2026-09-14)

§13이 「안 한 것」으로 올려 둔 세 항목이다. 결정과 그 결과만 적는다.

| # | 무엇 | 결정 | 상태 |
|---|---|---|---|
| 1 | NVDA 수동 검증 | **나중에 한다** — 윈도우 PC가 있고 그때 함께 돌린다 | ⏸ 문구를 사실에 맞췄다 |
| 2 | `flag_report` 호출 제한 | **지금은 안 한다** | ⏸ 원인 그대로, 피해 경로는 막힘 |
| 3 | 응급실·AED 좌표 | **공식 출처를 다 시도하고, 안 되면 추정하되 방법을 밝힌다** | ✅ 8건 채움 |

### 1. NVDA — 사이트가 하지 않은 일을 했다고 적고 있었다

`common.honesty.a11ySelfCheck`가 **「KWCAG 2.2 자체 점검 (자동 검사 + NVDA 수동 검증)」** 이었다. 앞 절반만 사실이다 — axe는 20경로에서 돌지만 NVDA 수동 과업 A·B·C는 한 번도 하지 않았다.

**접근성 서비스가 자기 접근성 검증에 대해 사실과 다르게 적는 것은 §5 정직성 문구 전체를 같이 무너뜨린다.** 문자열을 실제로 돌린 것만 적도록 고쳤다.

> KWCAG 2.2 자체 점검 (axe-core 자동 검사. NVDA 수동 검증은 아직 하지 않았습니다)

`08_accessibility_legal.md` §1.5에 **조건부 문구 규칙**을 넣었다 — 과업 A·B·C를 마치는 회차에 원래 형태로 되돌린다. 스펙이 「하고 나서 쓸 문장」과 「그 전에 쓸 문장」을 둘 다 갖는다.

### 2. `flag_report` — 이번 회차에서 하지 않는다

원인은 그대로다. `authenticated`가 「신원이 확인된 사람」이 아니라 **「세션을 가진 사람」** 이고, 익명 로그인이 켜져 있으므로 신고 버튼을 누르려는 사람은 예외 없이 자격을 얻는다. 마이그레이션 주석이 적어 둔 「`anon`을 막아서 해결했다」는 **막은 사람이 0명**이다.

**피해 경로는 이미 끊겼다**(§6.4의 50행 페이징 + 「신고 안 된 것」 보기). 관광지 6곳, 공개 홍보 전이라는 조건에서 공격자가 얻는 것은 「운영자를 성가시게 한다」까지다.

**할 때의 신호:** 제보가 실제로 쌓이기 시작하거나, 심사 후 공개 홍보를 할 때. 그때 `003_report_flags.sql`로 `(제보 id, 신고한 사람)` 유일 인덱스를 건다. 마이그레이션은 한 번 push하면 제자리 수정이 불가능하므로(§2.3) 미루는 비용이 아니라 **되돌릴 수 없는 것을 급하게 하지 않는 값**이다.

### 3. 응급실·AED 좌표 — 8건을 채웠고, 방법을 화면에 적었다

`content/facilities.json`의 병원 6행 + AED 3행이 전부 `coord: null`이라 `emergency_distance`·`aed_distance` 12개 사실이 영구 `unknown`이었다.

**공식 출처를 먼저 다 시도했다.**

| 시도 | 결과 |
|---|---|
| 공공데이터포털 `B552657/ErmctInfoInqireService` (응급의료기관) | `resultCode 30` — 이 계정이 신청하지 않은 서비스 |
| 공공데이터포털 `B552657/AEDInfoInqireService` (자동심장충격기) | `resultCode 30` — 〃 |
| 공주의료원·건양대 부여병원 공식 페이지 HTML에서 지도 위젯 좌표 | 좌표를 싣지 않는다 |

같은 data.go.kr 키가 기상청(`1360000`)에는 통하므로 키 문제가 아니라 **신청 범위 문제**다. 두 서비스를 새로 신청하면 승인 대기가 붙는다.

**그래서 결정대로 추정하되, 독립된 두 조회가 일치할 때만 썼다.**

| 시설 | 좌표 | 근거 |
|---|---|---|
| 충청남도 공주의료원 응급실 | 36.4592163, 127.1092005 | OSM `way/817841531` (`amenity=hospital`) · Nominatim 조회와 **27m 이내 일치** |
| 건양대학교 부여병원 응급실 | 36.2753039, 126.9016066 | OSM `way/367065989` (`amenity=hospital`) · Nominatim 조회와 **8m 이내 일치** |
| 정림사지박물관 로비 | 36.2792103, 126.9151940 | OSM `way/860922355` (`tourism=museum`) |
| 어린이박물관 | 36.2763114, 126.9190510 | OSM `way/366966605` 국립부여박물관 · **주소(부여읍 금성로 5)가 우리 기록과 일치** |
| 웅진백제역사문화관 | **채우지 않았다** | 어느 공개 출처에서도 못 찾았다. 무령왕릉의 `aed_distance`는 `unknown`으로 남는다 |

**주소 지오코딩이 아니라 이름으로 조회한 것이 중요하다.** 「무령로 77」로 조회하면 도로 중간점이 나오지만(세 후보가 1.5km에 걸쳐 흩어졌다) 「충청남도 공주의료원」으로 조회하면 OSM이 매핑해 둔 **건물 자체**가 나온다. `way/…`는 누구나 `openstreetmap.org`에서 열어 확인할 수 있다.

**방법은 세 곳에 적었다.** 근거 카드의 문장 자체(`충청남도 공주의료원 응급실 직선거리 1,627m`), 각 행의 출처 줄(OSM 요소 id + ODbL + 「직선거리이며 실제 이동거리가 아닙니다」), 그리고 안전시설 절의 설명(`place.safetyNote`).

> **문장 안에 「직선거리」를 넣은 이유.** 이 숫자는 방문자가 「도움이 가까운가」를 판단할 때 읽는 값이고, 산비탈을 가로지르는 직선은 올라가는 길보다 짧다. **읽히는 자리에서 방법을 밝히지 않으면 밝히지 않은 것이다.**

**결과 — 8개 사실이 `unknown`에서 벗어났다.**

```
공산성          응급실 직선거리 1,627m   이용 불가
무령왕릉과 왕릉원  응급실 직선거리   330m   확인됨
국립공주박물관    응급실 직선거리   816m   일부 가능
부소산성        응급실 직선거리 1,546m   이용 불가
정림사지        응급실 직선거리 1,152m   이용 불가 · AED 182m 확인됨
국립부여박물관    응급실 직선거리 1,559m   이용 불가 · AED  10m 확인됨
```

**점수는 오히려 내려간 곳이 있다. 그게 맞다.** `unknown`은 0.35로 계산되고 `unsupported`는 0.0이다. 응급실이 1.6km라는 것을 **알게 되면** 모르던 때보다 나쁜 값이 된다.

| 관광지 | 점수 | 커버리지 |
|---|---|---|
| 공산성 | 42 → **41** | 0.290 → **0.323** |
| 무령왕릉과 왕릉원 | 57 → **58** | 0.452 → **0.484** |
| 국립공주박물관 | 58 → **59** | 0.452 → **0.484** |
| 부소산성 | 47 → **46** | 0.400 → **0.433** |
| 정림사지 | 51 → **51** | 0.387 → **0.452** |
| 국립부여박물관 | 52 → **52** | 0.452 → **0.516** |

**라벨은 하나도 안 바뀌었다.** 커버리지는 6곳 전부 올랐고, 점수는 응급실이 가까운 곳에서 오르고 먼 곳에서 내렸다. 둘 다 참이다.

> **덧 — 오차 방향이 이번에는 안전한 쪽이었다.** 직선거리가 걷는 거리를 가장 크게 과소평가하는 곳은 산성인데, 공산성 1,627m와 부소산성 1,546m는 둘 다 1,000m를 넘어 이미 **가장 나쁜 구간**에 있다. 낙관적으로 잡힌 두 곳(무령왕릉 330m, 국립공주박물관 816m)은 평지 시가지라 직선과 도보가 거의 같다. **설계가 아니라 운이므로 문구는 그대로 둔다.**

---

## 13. 검증

```
pnpm typecheck        통과
pnpm lint             통과
pnpm test             160 passed (146 → 160)
pnpm validate:content  통과
pnpm build            통과
pnpm e2e              24 passed → axe 20경로 + 포커스 4 + 골든 3
pnpm ingest           exit 0, 경고 0건 (이미지 https 경고 제외)
GitHub Actions ingest 성공 — 2분, 전 단계, 스냅샷 자동 커밋
Vercel 배포       modu-baekje.vercel.app · 19개 라우트 200 · 사진 17/17 디코드
/api/revalidate   잘못된 비밀키 403 · 올바른 비밀키 200
```

늘어난 테스트 14건: `resolve-status.test.ts` 12건(§1의 문장들) + `suitability.test.ts` 2건(대체추천 고리, 필수 항목 전부 해당없음).

### 손으로 확인한 것

- **제보 흐름 전 구간.** 04번 §2.2가 「자동 검사가 하나도 없다」로 남겨 둔 것이다. 브라우저에서 공산성에 제보를 등록하고(익명 세션 생성 → insert → 확인 문구), 관광지 화면의 제보 절에 **즉시** 나타나는 것까지 봤다. e2e로는 덮지 못한다 — 그 스위트는 픽스처 빌드라 데이터베이스가 없다
- 모바일 390px 가로 스크롤 없음, 전 라우트 HTTP 상태, 전 화면 제목 단계, 영어 화면의 미표시 한국어 0건

### 안 한 것 — 셋 다 §12c에서 결정됐다

- **NVDA 수동 검증** — Windows PC에서 다음 회차에 한다. 그때까지 화면 문구가 그 사실을 말한다(§12c.1)
- **`flag_report` 호출 제한** — 원인은 그대로, 피해 경로는 막힘. 제보가 쌓이거나 공개 홍보를 할 때 `003`을 쓴다(§12c.2)
- **웅진백제역사문화관 AED 좌표** — 어느 공개 출처에도 없다. 무령왕릉의 `aed_distance`만 `unknown`으로 남는다. 나머지 8건은 채웠다(§12c.3)
